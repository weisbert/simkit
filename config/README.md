# Config examples

Templates users copy into their own project trees.

- `pvtproject.example.json` — rename to `.pvtproject` and drop at the root of a circuit project directory. Auto-discovered by the collector SKILL and the `pvt` CLI.
