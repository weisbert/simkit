# Project State

_Last updated: 2026-05-19 night (Phase 4 §1AXX dogfood session — 8 commits of UX/schema fixes from real-Maestro use; **HANDOFF priority 1: process-improvement discussion** — user explicitly flagged "太多问题没有被防御住，漏到我这里" and asked to discuss next session.)_

_Today's night session (8 simkit commits past last handoff `3b9cabb`). User switched from the synthetic `simkit_p3b_dogfood` Claude-created sandbox to a real production workarea `~/cadence_work/Test/workarea/simkit_1AXX/` targeting live `sim_yusheng/Test/maestro` (project name `1AXX`, session `fnxSession0`, 3 live corners TT scalar / TT_pvt multi-VDD multi-section / TT_2p5G LO sweep). Every commit responded to a specific user-surfaced bug or UX gap. DECISIONS #79 D1-D13 records the full arc; commits in order: `2b6dd68` history-node naming + run-name dialog, `f59e010` session auto-detect + empty `_file_abs` accepted, `e77f77b` project-name regex `[A-Za-z0-9_-]+`, `a1da05b` process-column adapter + right-click menus + New-Review MVP, `d0a5724` corners table 0-px rows (blockSignals bug), `041d548` FS watcher + Refresh + parse-error labels, `315c247` Measures Pull + Bundles tree group, `aa8318c` RunController crash (@pyqtSlot signature mismatch) + multi-process column round-trip. Tests **1502 → 1511 (+9 net)** — most bug fixes inverted/replaced existing tests rather than adding new ones; the 4 group-count rebases on tree_model tests accounted for 6 of the touched test cases._

_For next conversation, read in order: this italic block, then DECISIONS #79 D13 specifically (the 7 leak patterns + 3 common-causes the user wants to discuss), then DECISIONS #79 D2 (the SKILL `_file_abs` root cause still open). Memories worth pulling: `[[feedback_mock_match_production_shape]]` (recorded but not strictly executed during this session's agent dispatches — central to the process discussion), `[[feedback_pm_mode_verification]]` (PM-mode runtime-verify gate per-feature, also not consistently enforced)._

_**Open items going into next session (priority order):**_

_1. **Process-improvement discussion** (USER-REQUESTED, must do first). Quoting user: "太多问题没有被防御住，漏到我这里，你先把当前问题修了，我们下一个窗口讨论一下流程问题." The 7 leak patterns documented in DECISIONS #79 D13 are the empirical evidence. Target: a checklist or mandate format the orchestrator (next-session me) enforces on every agent dispatch + every "done" claim, so this class of bug doesn't reach user dogfood again._

_2. **SKILL `pvtCornersPull` _file_abs root cause** (D2 outstanding). Python validator now treats empty as informational, unblocking the user. But the SKILL side should resolve `_file_abs` per-section for multi-section model rows (current behaviour: gives up + writes `""` for any multi-section row). 5-10 LOC SKILL fix in `pvtCorners.il` + Tier-1 fixture for the multi-section pull path._

_3. **User-verified Run end-to-end** — `aa8318c` fixed the connect-time crash; user hadn't gotten to try Run after the fix when the session ended. Their first action next session should be to click [Run this review] in the GUI and confirm it actually launches Maestro through to completion. If anything's broken at the QProcess → orchestrator → pvt_save → ingest pipeline, that's still a class of "not caught by mocked tests" bugs._

_4. **Measures editor blockSignals audit** — Corners' 0-px bug (D4) was caused by `blockSignals(True)` suppressing rowsInserted. Measures editor uses similar bulk-insert patterns; needs audit + same `layoutChanged.emit()` defensive fix even if not currently broken. Plus a widget-level test mandate: every editor with a bulk-load API must have a pytest-qt test that asserts `viewport().visibleRegion().contains(rect_of_row_N)` or similar, not just `model.rowCount() == N`._

_5. **Set milestone right-click action** (Stage 4 §15 — currently grayed-out placeholder menu item)._

_6. **Snapshot → bundle template-aware reverse** (P3B v2 deferred, not Stage 4 critical) — currently `Pull from Measures` produces a flat raw_expression bundle; user can edit to add template structure manually._

_**Concrete state for next session resume:**_

_- `origin/main` = `<head-after-push>` if user pushes; current local head is `aa8318c` (8 simkit commits since `3b9cabb` last handoff). Working tree mostly clean (one untracked `logs_yusheng/` dir under simkit root is Cadence runtime trash, can ignore). DECISIONS handoff commit pending after this italic-block edit._
_- **Python 1511 / 1511 green** (32s). SKILL Tier-1 462/0/0 expected unchanged (only `pvtProject.il` touched this session; regex loosening verified via direct bridge probe — full Tier-1 not re-run because the change is mechanical)._
_- User's 1AXX workarea: `/home/yusheng/cadence_work/Test/workarea/simkit_1AXX/` — pre-populated with `.pvtproject`, 2 reviews (sanity_check + sanity_check_trans), 1 union (baseline.union.json pulled live, 3 rows), 21 builtin templates, no signal_groups, 0 ingested runs (empty DuckDB at .db/). 1 live-pulled bundle in `bundles/live_pulled_*` after the Measures Pull demo._
_- Maestro state: `sim_yusheng/Test/maestro` cell, session `fnxSession0`, 3 live corners (TT/TT_pvt/TT_2p5G), 2 enabled tests (Test + Test_trans). User has NOT actually run a sim through the GUI yet — that's open item #3._
_- Local `.venv` at `/home/yusheng/cadence_work/Test/workarea/simkit/.venv` unchanged (Python 3.11.13 + PyQt5 5.15.11 + pytest-qt 4.5.0)._



_Today's evening session (5 simkit commits past last handoff `7c2f252`). Repeated #77's 4-parallel pattern, file-disjoint scopes, no agent touches main_window.py — Phase-3 integration (orchestrator) does all the wiring after agents return. Agent A (~7m wall-clock) shipped loaders + tree_model + run_progress + corners_editor `set_project_root` (`ca09cab`); Agent B (~11m) shipped CLI `--gui-jsonl` + RunController + gui_events + orchestrator `progress_cb`/`cancel_check` (`99897de`); Agent C (~12m) shipped DiffController + DiffResultsModel + DiffTab + RunPickerDialog + results_tab Baseline pin (`2a147e6`); Agent D (~3m) shipped error_translation table (18 entries — expanded from spec's 9 to cover real SkillBridgeError categories) + ErrorTranslator QObject (`01c863f`). Phase-3 integration commit (`0eeab2d`) replaced all 5 log-only stubs in main_window.py with real BridgeWorker dispatch via a `_pending_ops: dict[req_id → {on_ok, on_err}]` callback dispatcher, added `set_bridge_worker` + `load_module` public surface, added Maestro-session QLineEdit in top bar persisted via additive `ModuleSession.session_name` field. Tests **1270 → 1502 (+232 net)** all green in 32s. Offscreen-Qt smoke verified 8 outbound signals dispatch cleanly + 3 ops queue through worker + clean teardown — needed to monkey-patch `QMessageBox.exec_` to a print-stub because the first smoke run sat 14min on a missing-session modal until user noticed ("为什么这个烟测这么慢"); root cause + smoke pattern pinned in DECISIONS #78 D5._

_For the next conversation, read in order: this italic block, then DECISIONS #78 (the 4-agent dispatch + Phase-3 integration + the 7 design decisions D1-D7). Memories worth pulling: nothing new from this session — the Stage 2 memories (`[[feedback-mock-match-production-shape]]`, `[[feedback-pm-mode-verification]]`, `[[reference-tier1-baseline]]`) all still apply. The red-zone trio (`[[project-red-zone-glibc-217]]`, `[[reference-cadence-qt-ldpath]]`, `[[reference-red-zone-shell-csh]]`) is critical for the next-session red-zone deploy._

_**Open items going into next session (priority order):**_

_1. **Red-zone deploy + dogfood smoke** (DO THIS FIRST). Stage 3 hasn't been physically verified on red zone yet. Home-Linux .venv smoke confirms wiring; red-zone needs a fresh `make_payload + deploy_venv + activate.csh` cycle to re-verify the Qt 5.15.3-shadowing fix from `[[reference-cadence-qt-ldpath]]` + the CRLF defense layer + the glibc 2.17 baseline all still hold. Test: `pvt gui --module <a real .pvtproject>` on the red EDA host, click Run on a real review, verify progress UI updates, verify results re-ingest after run completes, click Compare to pick a baseline run. This is the dogfood gate, not "tests pass"._

_2. **Stage 4: Milestone tagging (§15 right-click + autocomplete + left-tree Milestones group click-filter) + Wizard (§14 copy-as primary path + from-scratch wizard secondary) + §16 status strip + polish.** `runs.milestone` already in DuckDB v4 from #77 D2 so milestone is light. Wizard is the bigger piece._

_3. **§12 dogfood acceptance gate** — user completes one real signoff cycle (NDIV PDR or CDR pass) entirely inside the GUI on red zone. Until this clears, Tier-2 work (cross-module dashboard, charts, etc.) is locked._

_**Concrete state for next session resume:**_

_- `origin/main` = `<head-after-push>` if user pushes; current local head is `0eeab2d` (5 simkit commits since last handoff `7c2f252`: `ca09cab` Stage 3A + `99897de` Stage 3B + `2a147e6` Stage 3C + `01c863f` Stage 3D + `0eeab2d` Phase-3 integration). Tree clean after this handoff commit._
_- **Python 1502 / 1502 green** (32s). SKILL Tier-1 462 / 0 / 0 unchanged (no SKILL changes this session — pure Python GUI work)._
_- Local `.venv` at `/home/yusheng/cadence_work/Test/workarea/simkit/.venv` (Python 3.11.13 + PyQt5 5.15.11 + pytest-qt 4.5.0). Gitignored._
_- Smoke script at `/tmp/simkit_smoke.py` — synthetic .pvtproject + load_module + 8-signal dispatch. Use as the Phase-3 regression check when touching MainWindow wiring (don't commit; it's a dev tool)._
_- **Phase-3 integration contracts pinned** (DECISIONS #78 D2 + D4): MainWindow's `_pending_ops` dispatcher is the only path BridgeWorker results take into the UI; ErrorTranslator subscribes to `op_failed` in parallel for translation. Session lives in `ModuleSession.session_name` (additive v1 field), edited via `MainWindow.session_input` QLineEdit, surfaced as required for every bridge-touching handler via `_warn_session_required`._
_- **5 outbound editor signals all wired** (DECISIONS #78 D6): `results_tab.run_requested` → RunController + run-progress tab; `corners_editor.pull_requested` → queue_op pvt_corners_pull → load_union into editor; `corners_editor.push_requested` → editor_rows_to_union_rows → temp file → queue_op pvt_corners_push replace=True; `measures_editor.apply_requested` → serialize RenderedRows → temp file → queue_op pvt_measure_push replace=True; the 3 divergence-strip actions (show_diff/pull_overrides_sidecar/keep_sidecar) all log distinct lines._
_- **DiffController, RunController, ErrorTranslator** instantiated lazily inside `MainWindow.set_bridge_worker(worker)` so MainWindow stays runnable for unit tests without a worker (the 5 outbound handlers gracefully no-op + log when `_worker is None`)._
_- User's environment unchanged from last handoff: RHEL7-era glibc 2.17, csh interactive shell, Python 3.11.4 (red) / 3.11.13 (home), `$LD_LIBRARY_PATH` carries `/software/public/qt/5.15.3_xcb/lib` from Cadence (deploy_venv.sh handles it)._



_Today's session (4 simkit commits past last handoff `a61543b`, plus one sibling `skill_tools` commit `d284bd4`). Three concurrent threads landed in sequence:_

_**1. Skillbridge sbStart.il PATH-based python auto-discovery** (cross-repo, sibling `skill_tools` commit `d284bd4` pushed; simkit commit `b925e46` for docs checkpoint). Made `load("...sbStart.il")` zero-config — `sbFindPython()` walks PATH via `command -v` `||` cascade; resolution chain is `$SKILLBRIDGE_PYTHON` → `sbFindPython()` → `error()` with actionable message. Replaces a hardcoded `/usr/bin/python3` fallback that doesn't exist on red-zone EDA hosts. Side finding pinned to memory: `ipcBeginProcess` substitutes `$VARNAME` BEFORE handing to /bin/sh; `$(...)` substitution is unaffected. Memory `[[feedback-skill-ipcbeginprocess-var-mangling]]`. DECISIONS #75._

_**2. BridgeWorker refactor: signal-based dispatch + heartbeat actually works** (commit `f315180`). The "QObject::killTimer" warning on `pvt gui` close was just the visible tail of a deeper bug — `worker.run`'s blocking `Queue.get()` loop and `worker.start_heartbeat` were BOTH connected to `thread.started`; the first slot blocked the worker thread's event loop forever, so the heartbeat timer NEVER actually fired during runtime (spec §8.2 silently broken — bridge dot stuck on AMBER). Refactored to Qt-native signal-based dispatch: `_op_queued` (queue_op → _dispatch, QueuedConnection) replaces `Queue.Queue`; `_stop_requested` (stop → _cleanup) replaces sentinel; `initialize` slot creates heartbeat QTimer on worker thread; `_cleanup` slot stops it on the same thread before destruction. Plus: added module-level `simkit.skill_bridge.evalstring()` for the heartbeat probe. Tests 1190 → 1193 (3 new `CleanupTests`). Created home-Linux `.venv` (`python3 -m venv .venv && pip install -e .[dev,gui]`, gitignored) for local PyQt5 verification. DECISIONS #76._

_**3. Phase 4 Stage 2: 4-parallel UI subsystem dispatch + MainWindow integration** (commit `35a41d9`). 4 `general-purpose` subagents in parallel with file-disjoint scopes: A=§4 Results view (ResultsModel + ResultsTab, 26 tests), B=§7 Corners editor (CornersEditor with B4 mandate, 19 tests), C=§8 Measure bundle editor (MeasuresEditor with live `template_render` preview, 18 tests), D=§9a DuckDB v3→v4 (`runs.milestone VARCHAR` + `runs.partial_run BOOLEAN`, 7 tests). All 4 returned clean in ~1h wall-clock, zero merge conflicts (the "do not touch `main_window.py`" constraint held). Orchestrator integration: `right_panel` replaced with `QTabWidget` hosting the 3 new views in Tier-1 order (Results → Corners → Measures); signals wired to `_on_*_requested` log-only stub handlers (Stage 3 will swap these to BridgeWorker.queue_op + QProcess). 7 new integration tests pin tab order + signal routing. Tests 1193 → 1270 (+77). Live `pvt gui --safe-mode` smoke: 3 tabs render correctly, zero stderr warnings. DECISIONS #77. Three editor-side follow-ups deferred to Stage 3 (D3 of #77): union↔flat row adapter for `CornersEditor` (real `UnionRow` has `vars: dict[str, tuple]` + `models`, editor uses flat dicts), model-file path validation (needs project root context), `set_available_templates(dict)` form actually used (currently dual-form contract list-or-dict, only list-side tested in MainWindow stubs)._

_**Plus housekeeping (commit `4ecd583`)**: `scripts/PHASE4_DEPS_HANDOFF.md` deleted; content was fully folded into `scripts/README.md` at `d4e2662`. Nothing referenced it in production code._

_For the next conversation, read in order: this italic block, then DECISIONS #75 / #76 / #77 (the three sub-arcs above), then TODO.md Phase 4 (§4 / §7 / §8 / §9a marked DONE; §5 Run path + §6 Diff are next). Memories worth pulling: `[[feedback-skill-ipcbeginprocess-var-mangling]]` (SKILL shell-out `$VAR` gotcha — burns anyone writing new shell-out via `ipcBeginProcess`). The red-zone trio is still relevant for any red-zone bring-up: `[[project_red_zone_glibc_217]]`, `[[reference_cadence_qt_ldpath]]`, `[[reference_red_zone_shell_csh]]`._

_**Open items going into next session (priority order):**_

_1. **Stage 3: Run path (§5) + Diff (§6) + module loading** (NEXT BIG PUSH). The 3 Stage-2 stub handlers in `main_window.py._on_*_requested` are placeholder log lines waiting for: (a) `pvt run --gui-jsonl` subprocess + `GuiEventEmitter` + `RunController` (QProcess + JSONL stdout per spec §9.2) + cancel semantics (SIGTERM + 5s grace + SIGKILL + `partial_run` DB flag per §9.3), (b) `ModuleSession` actually loading a project into the editors (load union into CornersEditor, load bundle into MeasuresEditor, populate left tree from DuckDB), (c) `BridgeWorker.queue_op` wired in place of the log-only stubs. §6 Diff naturally pairs with §5 because both need the same module-session/project plumbing. Also lands the union↔flat adapter + model-file validation + dict-form template setter wiring._

_2. **Stage 4: Milestone tagging UI (§15) + Wizard (§14) + Polish + Tier-1 dogfood gate** — the gate is "user completes one real signoff cycle entirely inside GUI on red zone", not test-count milestones._

_3. **Error translation table (B5)** — spec §8.3 lists 7 SkillBridgeError categories that should map to plain-English user messages with Details disclosure. Mostly mechanical; can fold into Stage 3 or earlier._

_**Concrete state for next session resume:**_

_- `origin/main` = `<head-after-push>` (4 new simkit commits since last push at `f315180`: `35a41d9` Stage 2 + `4ecd583` deps-handoff cleanup + this handoff commit; plus `b925e46` checkpoint pushed earlier with #75). Tree clean._
_- **Python 1270 / 1270 green** (29s). SKILL Tier-1 462 / 0 / 0 unchanged (no SKILL changes this session)._
_- Local `.venv` exists at `/home/yusheng/cadence_work/Test/workarea/simkit/.venv` (Python 3.11.13 + PyQt5 5.15.11 + pytest-qt 4.5.0 + duckdb 1.5.2). Gitignored. Required for `pvt gui` local testing._
_- Red zone path: `/data/RFIC3/Hi1108V100_Pilot_C1Xplus/w84368867/workarea/simkit_deploys/...`. Last successful deploy was at `simkit_20260519_5e8538c`. Next deploy from current main will be hands-off + heartbeat will actually work + 3 tabs render — recommend a fresh red-zone deploy + smoke run before Stage 3 lands more code._
_- Sibling `skill_tools` repo at `../skill_tools/`: `main` at `d284bd4` (pushed) — carries the `sbStart.il` PATH-based python auto-discovery. Cadence-side users no longer need `setenv SKILLBRIDGE_PYTHON` as long as `python3` is on PATH (typically `module load python/3.11`)._
_- **BridgeWorker contract pinned for Stage 3** (DECISIONS #76 + spec §8): `worker.queue_op(func_name, **kwargs) -> request_id` is thread-safe; result delivered via `op_complete(request_id, result)` / `op_failed(request_id, BridgeError)` signals on main thread (queued connections). `worker.stop()` thread-safe; emits cleanup request. The heartbeat QTimer is owned by the worker thread; do not touch it from main._
_- **Stage-2 stub-handler signal contracts pinned** (DECISIONS #77 D2): `ResultsTab.run_requested(str)`, `CornersEditor.pull_requested()`, `CornersEditor.push_requested(list[dict])`, `CornersEditor.show_diff/pull_overrides_sidecar/keep_sidecar()`, `MeasuresEditor.apply_requested(list[RenderedRow])`. Stage 3 only swaps the `_on_*_requested` body in `main_window.py` — the contracts themselves are stable._
_- User's environment confirmed: RHEL7-era glibc 2.17, csh interactive shell, Python 3.11.4 (red) / 3.11.13 (home), `$LD_LIBRARY_PATH` carries `/software/public/qt/5.15.3_xcb/lib` from Cadence (the shadowing culprit, handled by deploy_venv.sh)._

_Today (later PM, 2026-05-19 — **Phase 4 §2 + §3 implemented + 4-fix red-zone bring-up cycle (DECISIONS #74)**: morning ended at `5dcb5a7` (Phase 4 §1 spec frozen, implementation not started). PM picked the recommended 2-parallel dispatch: Agent A → §2 (PyQt5/QtAwesome/pytest-qt added to requirements + lock refresh in clean /tmp venv + `scripts/PHASE4_DEPS_HANDOFF.md` handoff doc — `d546bba`); Agent B → §3 (`python/simkit/gui/` package with `ModuleSession` + `GuiAppState` + `BridgeWorker` singleton on dedicated QThread + 10s heartbeat + 5-zone MainWindow scaffold + `pvt gui [--module] [--safe-mode]` subcommand + 37 mock-based unit tests, mocking PyQt5 + skillbridge-shape-correct fixtures per `[[feedback_mock_match_production_shape]]` — `203bf75`). Both clean on first dispatch; 1190/1190. User pushed to GitHub for red-zone validation. Then 4 environmental bugs surfaced in sequence, each fixed before user could clear §3 GUI-launch gate: **(1) CRLF on red-zone bash** — Yellow Windows' default `core.autocrlf=true` baked CRLF into `.sh` files; red bash dies on `set -euo pipefail\r`. First attempt `3bcfba5` (.gitattributes + unpack_payload.sh sed-strip + `make_payload --no-wheels` for code-only iteration) was insufficient because yellow's already-CRLF working tree survives `git pull` (no re-checkout). Root-cause kill at `a50b4b2`: `make_payload.py` now normalizes CRLF→LF at PACK time via `add_recursive_normalized` (`tar.addfile` with BytesIO content rewrite for `.sh` / `.py` / `.il` / `.ils` extensions, counter that prints "[normalize] N files had CRLF") — bulletproof at producer regardless of yellow's working tree state. Three-layer defense now in place (`.gitattributes` preventive → make_payload generator → unpack sed-strip consumer). **(2) duckdb 1.5.2 wheel needs glibc 2.26+** — red zone is glibc 2.17, manylinux2014 only. Probe found `duckdb==1.2.2` is the last version shipping cp311 manylinux_2_17 wheels (1.3+ bumped baseline). simkit's duckdb API surface is just `duckdb.connect()` + plain SQL — safe to downgrade. Lock pinned `1.2.2`, `download_wheels.py` `DEFAULT_PLATFORMS` tightened to drop `manylinux_2_28_x86_64` (now `["manylinux2014", "manylinux_2_17"]`, both name the same baseline) — fail-loud if any future package drops manylinux2014 support. Verified full lock-set resolves under 2_17 (17 wheels, all glibc-2.17-compatible) + 1190/1190 passes on both 1.5.2 (current green env) and 1.2.2 (clean tmp venv probe). `5e8538c`. **(3) Cadence Qt 5.15.3 shadows wheel's Qt 5.15.18** — red zone `$LD_LIBRARY_PATH` carries `/software/public/qt/5.15.3_xcb/lib` from Cadence Virtuoso sourcing. Linux loader finds 5.15.3 before the wheel's bundled `PyQt5/Qt5/lib/libQt5Core.so.5` (5.15.18) → ImportError on symbol `_ZdlPvm`. User's diagnostic confirmed `ldd` resolving to Cadence path; manual `setenv LD_LIBRARY_PATH "$VIRTUAL_ENV/.../PyQt5/Qt5/lib:$LD_LIBRARY_PATH"` fixed it; window launched. Permanent fix at `62489e5`: `deploy_venv.sh` post-pip-install patches BOTH `.venv/bin/activate` (bash, `${LD_LIBRARY_PATH:+:...}` empty-guard) AND `.venv/bin/activate.csh` (csh, `if ($?LD_LIBRARY_PATH)` guard) with the prepend; PyQt5 added to smoke tests; final hint shows both bash + csh activate forms (red is csh). Also `app.py` `except ImportError` was lying — said "PyQt5 not installed" when it was loaded-but-symbol-failed — split into `ModuleNotFoundError` (exit 4, original install hint) vs `ImportError` (exit 5, prints real exception + LD_LIBRARY_PATH guidance referencing `/software/public/qt/5.15.x_xcb/lib` so EDA users immediately recognize it). **(4) Documentation gaps** — `scripts/README.md` didn't have `--no-wheels`, CRLF defense, Qt5 LD_LIBRARY_PATH, or glibc 2.17 baseline; troubleshooting still mentioned removed `manylinux_2_28`. All folded in at `d4e2662`. **Remaining: BridgeWorker timer cross-thread shutdown warning** (Qt thread-affinity bug from Agent B's implementation — `QObject::killTimer / ~QObject: Timers cannot be stopped from another thread` printed to stderr when `pvt gui` closes; GUI displays + works regardless). Deferred to next session because (a) Stage 2 agents will all touch BridgeWorker so worth fixing FIRST and (b) user moved past §3 gate. 6 commits this PM: `d546bba` §2 → `203bf75` §3 → `3bcfba5` CRLF v1 + --no-wheels → `a50b4b2` CRLF root-cause → `5e8538c` duckdb 1.2.2 → `62489e5` Qt5 LD path + ImportError split → `d4e2662` README. Python: 1153 → 1190 (+37 GUI tests). DECISIONS #74 records D1-D6 + the producer-vs-consumer defense-layer rationale + the glibc 2.17 baseline forever-rule. `[[project_red_zone_glibc_217]]` + `[[reference_cadence_qt_ldpath]]` + `[[reference_red_zone_shell_csh]]` added._)

_Today (post-Phase-3A-closeout) (**Phase 4 §1 GUI spec — DONE 2026-05-19 (DECISIONS #73)**: conversation-driven scoping that produced `docs/phase4_gui_spec.md` (558 lines, 22 sections) as the §1 design contract. Five workflow learnings overrode earlier assumptions: (a) **user has NEVER used the `pvt` CLI** — Phase 4 GUI is the FIRST real user entry point, not "wrap-an-experienced-CLI-user's-commands". 19 days of Phase 1-3A CLI dogfood was done by Claude on user's behalf; user verbatim "我就想用 GUI". (b) **"Design Review" = engineering noun, multi-module** — one DR cycle (PDR / CDR / FDR) spans NDIV + CP + LDO + …, each with multiple `.review.json` files. (c) **PDR/CDR/FDR gates need first-class support** — same modules revisited at each gate with possibly more corners / tighter specs; decided architecture: free-string `milestone` tag on runs row extending existing `★ starred` + Maestro history-lock infra (DECISIONS #65), NOT file-level explosion or naming convention. (d) **Diff is core, not auxiliary** — user verbatim "电路仿真没有对比就没有设计"; promoted from Tier-2 to Tier-1 first class. (e) **Cross-module visibility partially restored to Tier-1** — UX-agent caught that morning ritual is inherently cross-module; narrow single-line top-bar status strip ("Last 24h: 7 done / 1 running / 2 FAIL") added to Tier-1 (full dashboard stays Tier-2). Tier-1 final scope: 8 user capabilities (view results / run / diff / edit corners / edit measures / tag milestone / copy-edit review / from-scratch wizard) + 5 cross-cutting (status strip + bridge worker + error translation + module session persistence + heartbeat). Two parallel agents (architecture review + UX review) ran before spec was written; all 10 recommendations absorbed into binding mandates §2.1: A1 single BridgeWorker + queue + global busy state; A2 QProcess + JSONL stdout + SIGTERM+grace+SIGKILL cancel + partial_run DB flag; A3 QAbstractTableModel everywhere, ban QTableWidget; A4 ModuleSession dataclass + QSettings persistence + restart restores; A5 bridge heartbeat every 10s + green/amber/red dot + one-click restart-bridge; B1 narrow status strip (overrides Tier-2 defer); B2 "Run this review" in review header NOT buried in Run tab; B3 explicit Compare button + sticky baseline pin + tabbed diff (spec-delta vs netlist-delta separate); B4 corner editor mirrors Maestro Tools→Corners affordances (add/dup/enable/dropdown); B5 error translation layer with Details disclosure. Spec covers layout (single window, top bar + status strip + left tree + right tabs + bottom log), module session lifecycle (`.simkit/gui_state.json` per module + `~/.simkit/gui_app.json` global, schema v1), bridge layer (worker singleton + heartbeat + error-translation table), subprocess layer (QProcess + `--gui-jsonl` CLI flag for structured progress), diff workflow, corner/measure editors (live render preview right sidebar for bundles), wizard, milestone tagging, status strip query shape, CLI entry (`pvt gui [--module] [--safe-mode]`), tests (`test_gui_*.py` units + pytest-qt widget tests), deployment integration (existing 3-zone pipeline gets PyQt5+pytest-qt+QtAwesome added to requirements.txt + re-freeze + re-download wheels + re-bundle). DuckDB schema v3→v4 additive: `runs.milestone VARCHAR DEFAULT NULL` + `runs.partial_run BOOLEAN DEFAULT FALSE`. Suggested impl order: deps → app skeleton + bridge + heartbeat → view results → run path → diff → corner editor → measure editor → milestone+status strip → wizard → polish + dogfood gate. Acceptance gate: Phase 4 done only when user completes one real signoff cycle entirely inside GUI on red zone — not "tests pass". Files: `docs/phase4_gui_spec.md` (new, 558 LOC); DECISIONS #73 appended; TODO.md Phase 4 section appended with §1-§12 work breakdown; this PROJECT_STATE.md updated. Python 1153/0 + SKILL Tier-1 462/0/0 unchanged (no code touched today, only docs). Next session: Phase 4 §2 (deployment pipeline integration) or §3 (app skeleton) — both blocked on user confirming "go implement"._)


_Earlier today (post-v1.9 #2) (**Phase 3A v1.9 #3 — additionalArgs snap/restore + 4 closeout items (DECISIONS #69)**: PM-mode batch closing all remaining v1.9 candidates. (1) **Gap #1 — additionalArgs snap/restore**: both `_execute_ic_chained_item` and `TransPssIc.apply` now snapshot each test's prior `additionalArgs` via the v1.9 #2 primitive `pvt_runner_get_sim_option_val(test, "additionalArgs", session=session)` BEFORE installing the pre-run script; `finally` writes each captured value back per-test instead of the v1.3-era unconditional clear-to-`""` on a single `test_for_ic`. `_NO_SNAPSHOT` sentinel distinguishes "bridge wrapper missing" / "probe raised" from "probe returned `None`"; both branches fall back to clear-to-`""` for safety. `getattr` back-compat keeps pre-v1.9 mock bridges working. Live-verified on `fnxSession0` — pre-probe captured `Test='+nodeset /tmp/fc'` + `Test_trans='+ic /tmp/ic'`, sentinel write + restore round-trip byte-identical for both. Closes v1.3 known-gap #1. (2) **Gap #2 — per-test pre-run script support**: latent infra only — new `PreRunSpec.per_test_corner_to_arg` field + `write_per_test_pre_run_scripts` helper. Orchestrator stays on shared-map shape (no behavioral change yet); future multi-test consumer can opt in. (3) **Gap #3 — 3-item chain offline pytest**: `tests/test_orchestrator_ic_chain_3item.py` (322 LOC, 7 cases) pins A → B (ic_from A) → C (ic_from B) via mock bridge + tmp_path fixture; validates `history_by_item` propagation + per-corner IC resolution at each link + batch fallback when upstream history missing. (4) **Tracer log `SIMKIT_TRACE=1`**: env-gated `_trace()` helper in `naive_retry.py`, imported by `gmin_bump.py` + `trans_pss_ic.py`. One stdout line per `.apply()` showing `targeted` + `remaining_before`. Default OFF, zero production noise. (5) **sys.modules-None mock audit**: 5 greps over `tests/` all return zero hits — the v1.5 trap pattern from `[[feedback-pytest-sysmodules-mock-trap]]` is not present anywhere in the suite. Audit-clean. **Plus runTests.il atomic loader fix** (separate SKILL tooling deliverable): replaced single-fallback `defvar` block with 4-layer resolution (`_PVT_FORCE_ROOT` global → empty-string-safe env vars → upward cwd walk for marker → cwd default) + actionable warning when `pvtError.il` not found. Empty-string env-var bug also fixed (`(or "" X)` was returning `""` not falling through). Tier-1 460/2 from both `cwd=simkit` and `cwd=/tmp` — load-path independence verified. The 2 FAILs (`corners/collectRowNames/skips-missing-row_name` + dialog fixture cleanup race) are pre-existing per Agent B's run and orthogonal to this batch; queued as v1.9 #4. Files: Python +189 LOC across 5 production files + 619 LOC across 5 test files; SKILL +80 LOC in `skill/tests/runTests.il`. Tests: **Python 1115 → 1153 (+38, all green, 9.96s)**. PM-mode subagent dispatch: Subagent A (general-purpose) executed items 1-5 in parallel with Subagent B (general-purpose) on the runTests.il fix; zero file overlap. DECISIONS #69 records D1-D5 + alternatives rejected + the probe oddity finding + queued follow-ups._)

_Earlier (post-v1.9 #1) (**Phase 3A v1.9 #2 — auto-probe `baseline_value` via `pvtRunnerGetSimOptionVal` (DECISIONS #68)**: closes DECISIONS #63 D4 deferral. gmin_bump's `baseline_value` is now auto-probed from the live asi at apply-start instead of being hardcoded `"1e-12"` — corrects for projects that manually loosened gmin in the Options form, where the v1.7 hardcoded default would have silently restored to stock-Spectre between per-corner overrides. New SKILL helper `pvtRunnerGetSimOptionVal(sess, testName, optionKey)` reads any Spectre simoption from a specific test's asi via the per-test asi path (`axlGetToolSession + asiGetSession + asiGetSimOptionVal`). Python wrapper `pvt_runner_get_sim_option_val(...) -> Optional[str]` translates the SKILL `pvt_runner_no_option` category to Python `None` (option not set is the expected case for things like `additionalArgs` on fresh sessions); other SKILL errors (`pvt_validation` / `pvt_runner_no_session`) still raise. `_resolve_baseline_value(ctx, option_name)` resolves with order **explicit sidecar > live probe > DEFAULT_BASELINE**, with source tag (`"sidecar"` / `"probe"` / `"default"`) appended to apply-summary notes for debug. `getattr(bridge, 'pvt_runner_get_sim_option_val', None)` keeps back-compat with existing mock bridges (they get default-fallback naturally — no test updates needed). **Live-verified on `fnxSession0`**: direct bridge probe `(Test, gmin)` → `'1e-12'`, `(Test, reltol)` → `'1e-3'`, absent option → `None`, missing test → raises `pvt_runner_no_session`; gmin_bump.apply() with no sidecar baseline → probe invoked → rendered script carries `asiSetSimOptionVal asi "gmin" "1e-12"` → notes show `baseline=1e-12 from probe` → corner-enable byte-identical entry/exit. **Side finding**: both `Test` and `Test_trans` currently carry stale `additionalArgs` values from old probes (`"+nodeset /tmp/fc"` / `"+ic /tmp/ic"` — pre-v1.3 CLI syntax), confirming v1.3 gap #1 (additionalArgs snapshot/restore) is a real concern; with this primitive in place, the gap-#1 fix becomes cheap. Tests: +7 gmin_bump auto-probe cases (probe-value/sidecar-wins/probe-None-falls-back/probe-raises-falls-back/first-failed-test/custom-option-name/no-probe-method-on-bridge) + 5 bridge wrapper cases + 8 SKILL Tier-1 cases. Python 1103 → 1115 (+12); SKILL Tier-1 +8 registered. DECISIONS #68._)

_Earlier (post-v1.8 #5) (**Phase 3A v1.9 #1 — `pvt corners push --replace` (DECISIONS #67)**: closes v1.2 #2 / DECISIONS #54 #8 — v1.1 dogfood's "pushed 3 rows, got 6 corners" surprise. Opt-in `--replace` flag on `pvt corners push` (default kept as v1 ADD-semantics for back-compat). SKILL: `pvtCornersPush` gains `?replace nil` keyword; when set, enumerates live corners via `axlGetCorners(sdb)`, computes drop set = live \ sidecar names, deletes each via `axlGetCorner + axlRemoveElement` (DECISIONS #54 #8 vetted path), then pushes. Wipe-protection guard refuses `replace=t with 0 valid rows` to catch accident scenarios — runs BEFORE dry-run short-circuit so `--dry-run --replace` against a malformed 0-row sidecar still surfaces the error. Python `pvt_corners_push` gains `replace: bool = False` kwarg; passed as SKILL `replace=t` only when truthy. CLI `--replace` flag; output marker concatenates `dry-run, replace` when both set; help text references DECISIONS #54 #8 so the surprise has a paper trail. **Live-verified on `fnxSession0` end-to-end**: 7-step probe — baseline pull (TT/TT_pvt/TT_2p5G) → ADD-push 4 extras → 6 unique rows ✓ → REPLACE-push same extras → 4 rows ✓ → rows[] refused (earlier gate) ✓ → rows-without-row_name refused (new wipe-protection gate, my exact error msg) ✓ → live table unchanged after refusal ✓ → dry-run + replace = no mutation ✓ → restore baseline via --replace push → live = 3-row baseline at exit ✓. Two side discoveries (out of scope but flagged): `load_union` rejected the live pull's `_file_abs=""` artifact (worked around by using raw `axlGetCorners` in probe); pvtJson treats `"rows": []` as effectively absent so the new wipe-gate is reached only via "rows present but all malformed" (rows-without-row_name case) — both safely refuse without mutation. Removal API pick `axlRemoveElement` over `axlRemoveCorner`/`axlClearCorners`/`axlDeleteCorner` (all exist as symbols but are C built-ins with no dogfood-vetted call path; `axlRemoveElement` is the proven v1.1 workaround). Tests: 3 bridge cases (replace kwarg pass-through + omit-when-false + combine-with-dry-run-and-session) + 3 CLI cases + 6 SKILL Tier-1 cases on the new `_pvtCornersCollectRowNames` helper. Python 1097 → 1103 (+6); SKILL Tier-1 +6 registered. DECISIONS #67 records D1-D5 + alternatives rejected + probe discoveries._)

_Earlier (post-v1.8 #4) (**Phase 3A v1.8 #5 — `trans_pss_ic` strategy (DECISIONS #66)**: implement the second built-in intervention strategy on top of the v1.6 chain framework. Conception turned out simpler than DECISIONS #52 originally sketched: the v1.3 `ic_from` mechanism (DECISIONS #57) is exactly the IC-injection path we need; `trans_pss_ic` becomes its **on-failure variant** — pre-run-script writing `readns/readic="<path>"` into per-corner `additionalArgs`, identical SKILL path, fired only when corners FAIL. No new SKILL surface, no `asiChangeAnalysis` probe needed (the original v1.1 deferral becomes moot). (1) **`simkit.strategies.trans_pss_ic.TransPssIc`** (~280 LOC): sidecar params `source_item` (required), `file` (`"ic"`/`"fc"`/`"dc"`, default `"ic"`), `mode` (`"readic"`/`"readns"`, default `"readic"`), optional `subdir` + `test_for_ic` overrides. Looks up `source_history` via `ctx.history_by_item[source_item]`; resolves per-corner IC path via `ic_source.resolve_ic_path` against `<results_root>/<source_history>/<idx>/<test>/<subdir>/spectre.<kind>`; pulls live union via `bridge.pvt_corners_pull` for sub-corner names + 1-based explode indices; uses the v1.7 A6 safe-write shape (`PreRunSpec.baseline_value=""`) so partial-row FAIL sets don't leak the IC arg across shared worker-VM asi. (2) **`StrategyContext` gains two optional fields** — `history_by_item: Mapping[str, str] | None` + `pvtproject_path: Any` — and `_run_strategy_chain` plumbs both through. naive_retry / gmin_bump don't read them; additive change. (3) **Live-verified on `fnxSession0` via `/tmp/probe_transic_v18.py`**: stubbed `pvt_runner_run` (mechanics-only verify; actual axlRunAllTests + per-corner script-fires path proven by v1.3 + v1.7); FAIL=TT_pvt_3 → strategy pulled live 3-row table (TT/TT_pvt-with-6-sub/TT_2p5G), resolved TT_pvt_3 = 5th explode index, found `simkit_verify/5/Test/netlist/spectre.ic` on disk, rendered script with safe-write shape (`asiSetSimOptionVal asi "additionalArgs" ""` STEP 1 + conditional readic overlay STEP 2), installed on Test only (only the failed test), narrowed corner-enable to TT_pvt row, ran stub, restored corner-enable byte-identical, reattached prior pre-run script. Bridge call order: `pull → snap → get_prerun(Test) → install_prerun(Test, ours) → restore(FAIL-only) → [run stubbed] → restore(orig snap) → install_prerun(Test, prior)`. (4) **Two probe-environment learnings recorded**: synthetic .pvtproject needs `dbRoot` field for bridge `_prep` to accept it (caught the first empty-pull); strategy must pass `pvtproject_path` as `Path`, not `str`, because production `_prep` does `.parent` on it (mock bridge accepted strings, so unit tests didn't catch — instance of [[feedback_mock_match_production_shape]] still being subtle even with the discipline applied). Both fixes 1-line. (5) Tests: `tests/test_strategies_trans_pss_ic.py` 28 cases covering param validation + orch-injection + happy path + teardown + IC-resolution edges + live-pull failures + sub-corner indexing + registration. Mock bridge's `pvt_corners_pull` writes a synthetic valid union JSON to the target so `load_union` runs the real code path. Python 1069 → 1097 (+28). SKILL Tier-1 unchanged (pure-Python feature). DECISIONS #66 records D1-D6 + alternatives rejected + the asi-probe-moot framing._)

_Earlier (post-v1.7 followups) (**Phase 3A v1.8 #4 — `pvt star` + Maestro history-lock sync (DECISIONS #65)**: user asked for a way to preserve important sims; investigation found Maestro already exposes `axlSetHistoryLock` / `axlGetHistoryLock` (adexlSKILLref.pdf p.425/433) and `maeSetHistoryLock(name, t/nil, ?session)` — and `fnxSession0` already had 3 reference fixtures (`simkit_verify` / `simkit_simerr` / `simkit_Rtime_err`) **manually locked** by the user as standing Tier-2 references. So the workflow concept was already half-deployed; we just needed the tool side. (1) **Schema v2 → v3**: `runs.starred BOOLEAN DEFAULT FALSE` (DuckDB rejects NOT NULL on ALTER ADD, so nullable + Python `bool()` coercion). Migration applied to live p3b_dogfood DB; 18 existing rows backfilled. (2) **Bridge wrappers**: `pvt_runner_set_history_lock(name, lock, *, session)` wraps `maeSetHistoryLock` via `evalstring` returning "T"/"nil" string for clean decode; raises `SkillBridgeError("lock_failed", …)` on nil. `pvt_runner_get_history_lock_map(*, session)` two-step: cache hsdb, then `axlGetHistory + axlGetHistoryEntry + axlGetHistoryLock` inside a `(let)` emitting tab-separated `name\tT|nil\n` lines. `_escape_skill_string` guards literal injection. (3) **`simkit.star` core**: `set_run_starred` (idempotent UPDATE returning `StarResult(action='set'|'cleared'|'noop')`), `compute_sync_plan(direction, db_rows, maestro_lock_map)` as pure function so dry-run is free, `apply_sync_plan` takes a `set_history_lock` callable so CLI tests can mock without a live bridge. `load_db_rows` groups by history_name with OR-fold of starred (multiple DB runs sharing a history → any starred forces lock). (4) **CLI**: 3 new verbs — `pvt star <run_id> [--no-push]` / `pvt unstar <run_id>` / `pvt sync-stars (push|pull) [--dry-run]`. `pvt list` gets a `★` column (width 1, prepended) + `--starred-only` filter; JSON unchanged. Single-run path writes DB first then optionally pushes, so a Maestro failure still leaves intent recorded — user re-runs `sync-stars push` once Maestro reachable. (5) **Live-verified on fnxSession0**: `pvt star 6704c1ac` (history `v17_gmin_demo__gmin2`) → bridge confirms lock=T; `pvt list --starred-only` shows ★; `pvt unstar` → bridge confirms lock=nil. The 3 pre-existing reference locks intact through every step. DECISIONS #65 records D1-D4 picks + alternatives rejected + future hook ("when `pvt forget` lands, default-block deletion of starred runs"). Tests: +17 `test_star.py` (core), +8 `test_skill_bridge.py` (lock wrappers), +14 `test_cli_star.py` (CLI + list column + filter). Python 1030 → 1069 (+39). SKILL Tier-1 unchanged (pure-Python feature; lock API already in adexl/mae namespaces)._)

_Earlier today (2026-05-18 EOD — **Phase 3A v1.8 #3 — `pvt list` history column, ex-phantom (DECISIONS #64)**: investigated the v1.5-reported "run.json `history_name: None`" before scheduling SKILL work, per [[feedback-pm-mode-verification]]. Found three things: (1) `_pvtCollAutoCapture` writes `histName` into `tab["history_name"]` and `_pvtCollBuildEnvelope` puts that table at `env["run"]` — so `d["run"]["history_name"]` has been correct since v1.5 PM (8e882e98 → 6704c1ac all carry valid names). (2) `runs.history_name` column populated for every DB row since same point. (3) The "None" report was a `dict.get()` misread against the top-level envelope (`[schema_version, run, results, artifacts, output_specs]` — no top-level history_name; .get returned None for the missing key, field lives nested under `run`). Real gap was display: `pvt list` table headers were `[run_id, timestamp, project, testbench, label, note]` — no history column, so users couldn't correlate a DB row to a Maestro history without `--json`. Fix: inserted `"history": 22` into `_COL_WIDTHS` between run_id and timestamp; appended `"history"` to headers + `r.history_name` to cells in `_print_table`. `--json` output unchanged. 2 new tests: `test_table_shows_history_column` + `test_table_truncates_long_history_with_ellipsis` (40-char name → `v17gmin_v17_gmin_demo…`). DECISIONS #64 records the phantom finding + alternatives rejected (annotate diff header / opt-in flag / no-op). Python 1028 → 1030 (+2). SKILL Tier-1 unchanged. Smoke-tested against live dogfood DB: 6704c1ac → `v17_gmin_demo__gmin2`, 380d183c → `v17gmin_v17_gmin_demo…` (truncated), all visible at a glance._)

_Earlier today (2026-05-18 EOD — **Phase 3A v1.7 — `gmin_bump` strategy + worker-VM asi state-leak fix (DECISIONS #63)**: first real intervention strategy plugged into the v1.6 chain framework. (1) **`simkit.strategies.gmin_bump.GminBump`**: uses the v1.3 pre-run-script mechanism (DECISIONS #57) to inject `asiSetSimOptionVal asi "gmin" <bump>` per-corner inside the worker VM — bumped floor lands in that corner's netlist only, user's main-session GUI Spectre Options stays untouched. Each `apply()` walks one step of a configurable `ramp` (default `[1e-11, 1e-10, 1e-9]` — 10× / 100× / 1000× looser than the Spectre `1e-12` baseline). Sidecar params: `ramp`, `option_name` (default `"gmin"`, override for UltraSim/AFS), `baseline_value` (default `"1e-12"`), `max_attempts` (default 3). (2) **A5 Phase 1 caught a state-leak bug**: worker-VM asi session is **shared across sub-corners in the same sweep row** (NOT per-sub-corner-fresh as A1 assumed). Initial probe targeting only TT_pvt_3 with `9.99e-10` left TT_pvt_4 and TT_pvt_5 carrying the bumped value because the `(when entry ...)` guard skipped the asi write for unmapped corners — but didn't restore baseline either. (3) **A6 fix**: `PreRunSpec.baseline_value` field; when set, renderer emits a different body that resolves `asi` UNCONDITIONALLY and writes `baseline_value` FIRST on every firing, THEN conditionally overlays the per-corner override. ic_from's existing shape (`baseline_value=None` default) is unchanged — its map covers every sub-corner so leak is unreachable. (4) **A7 re-verify**: same single-target probe POST-FIX, TT_pvt_4/5 now back to `1e-12`; TT_pvt_3 still gets `9.99e-10`; both `Test` and `Test_trans` netlists agree. Two-phase live verify cycle on `fnxSession0` end-to-end clean. Test additions: `tests/test_strategies_gmin_bump.py` (30 cases) + `tests/test_pre_run_script.py` +4 cases. Python 996 → 1028 (+32). SKILL Tier-1 unchanged (pure-Python feature + safer renderer). DECISIONS #63 records D1-D5 design + alternatives rejected + the asi-sharing discovery for future strategy authors. v1.6 commit also pushed to origin/main earlier in this session. v1.8 candidates: `trans_pss_ic` (same plug-in shape, `asiChangeAnalysis` probe), auto-probe baseline_value, per-attempt tracer log._)

_Earlier today (2026-05-18 PM late — Phase 3A v1.6 — per-corner FAIL detection + strategy chain dispatch (closes v1.2 #3): the `strategies: [naive_retry]` field that's been parsed but unread since v1 is finally consumed. (1) **`simkit.failures`**: new pure-Python module aggregating `results` rows per corner into `FailedCorner(corner, reasons, sample_test, sample_output)`. Three reason codes: `spec_fail` (v1.4 specs), `sim_status_fail` (`__sim_status__` sentinel `failed|no_convergence`), `eval_err` (excluded from auto-retry — calc-expression bug, retrying doesn't help). 13 Tier-1 cases via synthetic DB inserts. (2) **`naive_retry` rewritten**: snapshot live corner-enable → narrow to FAIL set → restore (target) → `pvt_runner_run` → restore (original) in `finally`. Sub-corner names (`TT_pvt_3`) → row names (`TT_pvt`) via longest-prefix match; mixed scalar + sub-corner FAILs handled. 11 cases (7 base + 4 mapping). (3) **`_run_strategy_chain`**: post-PvtSave + ingest → query FAIL → iterate `item.on_failure.strategies` × `max_attempts` → each attempt = apply + PvtSave + ingest + re-query + narrow `remaining` + classify `recovered`/`unchanged`/`gave_up` → chain stops when remaining drains or all strategies exhaust. Unknown strategy names skipped with note (forward-compat with future `gmin_bump`). 8 mock-bridge integration cases. (4) **`ItemResult` extended** with `strategy_attempts: tuple[StrategyAttempt, ...]` (audit log) + `final_failed_corners: tuple[str, ...]` (post-chain residue). CLI `pvt run` summary now shows `[FAIL (N)]` and `naive_retry #1 → unchanged  targeted=...  remaining=...`; exit 6 when `final_failed_corners` non-empty even if `completed=True`. **Live-verified on fnxSession0 in 2 phases**: phase 1 = real `pvt run` with the v16_dogfood review caught 6 eval_err on Rtime_clkout → `final_failed_corners=(TT_pvt_0..5)`, naive_retry correctly NOT invoked (per D2), exit 6, snapshot_restored=True; phase 2 = forced-retry probe (custom `query_failed_cb` relabelling eval_err as spec_fail) drove the FULL chain: snapshot → narrow → run retry → ingest → re-query → outcome=unchanged → restore, ALL bridge calls verified, corner + test state byte-identical to entry. Two bugs caught during dogfood — `pvt_save` returns the run.json **file** path not the dir (my `_load_run_id` did `dir/"run.json"/"run.json"`), and run.json shape has `run_id` nested under `["run"]` not top-level. Both fixed + mock fixtures updated to mirror production shape so future tests catch this class of bug. DECISIONS #62 records D1-D4 (corner-granularity, eval_err policy, strategy/orchestrator separation, sub→row mapping) + alternatives rejected. Python 963 → 996 (+33). SKILL Tier-1 unchanged (pure-Python feature). New ingested runs: `c3538dc3`, `172edd50`, `486dcbb8`, `9b4180ea`, `b9df339e`. TODO v1.7 candidates seeded with `gmin_bump` + `trans_pss_ic` implementations (chain plumbing now in place — these become isolated drop-ins)._)

_Earlier today (2026-05-18 PM, before v1.6 — Phase 3A v1.5: CLI wired to `execute()` + first real `pvt run` dogfood + v1.2 #1 leftover (rdb-walker discriminator) finally closed): today's session in 3 deliverables. (1) **`pvt run` CLI ↔ orchestrator wiring**: `python/simkit/cli/run.py` was stubbed to exit-5 "not yet implemented"; threaded to `execute()` with `--session`/`--no-push-union`/`--history-prefix` flags + PVT_SESSION env-var fallback + auto-walked .pvtproject from review.json's parent dir. Exit codes: 0=all-ok, 6=partial-completion, 7=bridge-import-fail. 6 new live-mode tests with mocked execute() (15/15 in test_cli_run.py). (2) **First real `pvt run` dogfood on `fnxSession0`** authored a 2-item review (`Test` + `Test_trans` against baseline union); first attempt wedged for 15min (pytest's `mock.patch.dict(sys.modules, {"simkit.skill_bridge": None})` doesn't block `from simkit import skill_bridge` once cached on the parent pkg — pytest leaked a real SKILL call with session="sessX", pkill -9 mid-cleanup half-wedged the VM; recovered via (pyKillServer)/(pyStartServer); lesson pinned in [[feedback-pytest-sysmodules-mock-trap]]); second attempt revealed F1 (validator `pending` rejection) + F2 (axlGetRunStatus returns (0,0) before Maestro queues → PvtSave dumps half/empty). (3) **v1.5 F1 + F2 fixes**: F1 = 7-line whitelist update in `validate.py` (`pending` is a legit collector status); F2 = new SKILL `pvtRunnerCountRunning(sess)` walks the current-history rdb for non-terminal `tst->status` rows + Python state machine AND-s it with `axlGetRunStatus`; opt-in `min_running_observed` kwarg for slow-queue cases. Live-verified end-to-end TWICE on fnxSession0: 0 pending/running rows in either run.json dump (vs pre-F2: 6 pending sentinels in item 1). DECISIONS #61 records the discriminator design + retracts v1.2 #1 alternatives a/b/c. Python 949 → 963 (+14: 1 validator + 6 CLI + 7 state-machine). SKILL Tier-1 434 → 446 (+12 mostly v1.5 helper). New ingested runs: `f5091388`, `1303dff8`, `6c54aa6e`, `3590b35b`, `eaceb87b`, `8e882e98`. Out of scope (queued): run.json `history_name: None` (pvtCollect PvtSave doesn't write histName into envelope — 1-line SKILL fix)._)

_Earlier today (2026-05-18 PM, before v1.5 — DECISIONS #60: skillbridge worker-VM socket-steal ROOT CAUSE + zero-click batch unlocked):_  (traced the "post-axlRunAllTests focus loss" pattern that was blamed on Maestro window stacking since DECISIONS #58 — actual root cause is axl worker virtuoso processes (`runICRP<N>` → `virtuoso -axlChildIdFlag <N>`) loading `.cdsinit` → `sbStart.il` → `pyStartServer`, which spawns a SECOND python_server binding `/tmp/skill-server-default.sock` (last-bind-wins). Fix in sibling `skill_tools/skillbridge/sbStart.il`: detect axl worker via `grep -aq axlChildIdFlag /proc/$PPID/cmdline`. Live-verified end-to-end: full v1.4 dogfood runs hands-off. DECISIONS #59's v1.4.1 polymorphism followup is CANCELLED.)

_Earlier today (2026-05-18 AM — Phase 3A v1.4 baseline-corner preservation, still valid):_  (**LANDED + DOGFOOD-VERIFIED on fnxSession0**: schema field `ReviewItem.baseline_corner` (override) + Python picker `_pick_baseline_corner` (auto-select first scalar / honor override / hard-fail when neither resolves) + snapshot-modify-restore via existing `pvt_runner_snapshot/restore_corners_enable` bridge helpers in `_execute_ic_chained_item`. Live dogfood `v14dog_pss_v14_1779072455_1` on pathological state `[(TT, False), (TT_pvt, True), (TT_2p5G, False)]` produced subdir 1 = TT (1 rf018 include, matches `simkit_verify` baseline) instead of subdir 1 = nom (24 rf018 includes); subdirs 2-4 = TT_pvt_0..2 with v1.3 readic injection intact; corner state restored byte-identical at end. DECISIONS #59 records full design + verify proof. Python 931 → 949 (+18: 7 picker + 4 orchestrator wiring + 7 schema). SKILL Tier-1 unchanged (pure-Python feature). Known v1.4.1 follow-up: extend `Snapshot/RestoreCornersEnable` SKILL helpers to accept sdb int per DECISIONS #58 polymorphism pattern, so the in-finally restore survives post-axlRunAllTests focus loss without requiring user re-click._)

_Earlier (2026-05-17 PM) (**Phase 3A v1.3 readic syntax + sdb-cached bridge — END-TO-END VERIFIED on `fnxSession0`**: morning handoff said "final readns retry pending"; today closed it. Diagnostic pre-run on TT_pvt sweep-row confirmed `axlGetCornerNameForCurrentPointInRun` emits `TT_pvt_0..5` (one fire per sub-point, VDD inner-sweep handled inside Spectre). Correctly-scoped v1.3 retry landed 6 distinct `readic="/tmp/simkit_dogfood_TT_pvt_X.ic"` lines into the 6 sub-points' `simulatorOptions options` blocks; Spectre completed 0 errors. The earlier retry that "failed" had been wrongly scoped to single-point TT corner. Companion infrastructure: `skill_bridge.get_sdb(name)` + polymorphic `sess` arg in `pvtRunner.il` (DECISIONS #58) bootstraps once + bypasses Cadence's window-focus-keyed name-resolution thereafter — no more per-call "please click Maestro Assembler". Wedge-detection in `get_sdb` translates 3 bridge failure modes (unpack-wedge / server-dead / focus-lost) into actionable `SkillBridgeError` messages. A/B test cleared the serial-dispatch misattribution: the ~1s per-sub-point serial dispatch is **Maestro's local sim default**, identical with/without pre-run script attached — not a v1.3 cost. The companion multi-rf018.scs finding was WRONG and was retracted 2026-05-18 after user pushback: `simkit_verify` (manual run) subdir 1 = real `TT` corner with 1 rf018 include; v13_retry2 subdir 1 = `nom` (no corner applied) with 24 rf018 includes spanning every section. Root cause: `_prep` over-disables scalar corners leaving only the sweep-row enabled → Maestro auto-inserts a `nom` baseline subdir → `nom` has no active corner → netlister falls back to all-section include. Section count is per-project (testmachine=1, company=3, others 2-4 — see [[project_baseline_section_count]]) so symptom shape varies; the only true anomaly is the 24-line nom. Fix queued as v1.4 (Option A4: preserve one scalar corner as baseline in `_prep`, auto-pick first enabled scalar, sidecar `baseline_corner:` override). Python 931/0 unchanged (this PM was infrastructure + verification work; no new test surface). SKILL Tier-1 434/1 → +0 (helpers were modified, not added). 7 commits ahead of origin/main not pushed._)

_Earlier today (2026-05-17 AM — superseded by above but kept for chronology): **Phase 3A v1.2 + v1.3 cross-item IC piping — LANDED + LIVE-VERIFIED end-to-end on fnxSession0**: User asked to prioritise the trans→PSS IC chaining workflow ("不同的corner读取不同的ic condition") over the v1.2 polling/replace items. Built across 3 architectural stages, last 5 commits: `503fa0b` stage-1 (ic_from schema + IC injection via additionalArgs); `1296cc4` stage-2 (per-corner submit; correct but N Maestro histories — bad GUI UX); `61b554a` v1.3 (single-batch + Maestro pre-run script — ONE history, GUI consolidated); `fc574bc` v1.3 fixes (worker-VM SKILL quirks: `let* → let+setq`, `cons → list`); `efb4cac` v1.3 syntax fix (additionalArgs is appended into netlist's `simulatorOptions options` block, NOT spectre CLI — emit `readns="<path>"` / `readic="<path>"` not `+nodeset`/`+ic`). End-to-end live evidence in Interactive.2 dogfood: ONE history with 3 per-corner subdirs, each `input.scs` had a DIFFERENT per-corner IC path, Spectre completed successfully. Final readns-syntax verify pending (high confidence; uses engineer's manual-flow syntax verbatim). Python 871 → 931 (+60). SKILL Tier-1 407/1 → 434/1 (+27 across IC source + pre-run script helpers). DECISIONS #57 grew through 3 stages in-file._)

## Current phase

**📍 NEXT SESSION'S TASK: Phase 4 §1 spec (PyQt5 GUI, module-centric).** Direction is locked in (see `[[project_phase4_gui_direction]]` memory), spec doc `docs/phase4_gui_spec.md` is NOT yet written. Open decisions remaining: 6 from earlier ASCII mockup discussion (project switching mechanism, history view organization, multi-window support, run progress UX, bundle preview placement, diff trigger). Plus 2 deep questions waiting on user (review-creation frequency, module-switching frequency). When implementation starts, first step is adding `PyQt5` + `pytest-qt` + `QtAwesome` to `requirements.txt`, re-freezing the lock, then re-downloading wheels + rebuilding tarball + redeploying.

**🚧 Phase 4 prep — 3-zone deployment pipeline + iteration helpers + auto-detect DONE 2026-05-19 (DECISIONS #72, plus 3 same-day followups).** Before starting Phase 4 GUI, built the home Linux → yellow Windows → red Linux migration plumbing. Six deliverables in commit `8686c9c`: `pyproject.toml` (replaces implicit PYTHONPATH=python/ convention; defines `pvt = simkit.cli.__main__:main` console_script entry; `[dev]`/`[gui]` optional extras); `requirements.txt` (top-level: duckdb 1.5.2 + skillbridge 1.8.0); `requirements-dev.txt`; `requirements.lock.txt` (full freeze via `pip freeze --all`, 10 packages including setuptools+wheel as build backend deps); 4 scripts in `scripts/` (`download_wheels.py` forces manylinux/cp311 wheels from Windows; `make_payload.py` cross-platform Python-tarfile bundler with `.manifest.txt` SHA256; `unpack_payload.sh` SHA256-verified; `deploy_venv.sh` full-isolation venv + `--no-build-isolation` editable install + 4 smoke tests). Followup `ab7be9f` added 2 iteration helpers: `deploy_venv.sh` now atomically maintains `<DEPLOYS>/current` symlink (with `--no-current` opt-out); new `scripts/cleanup_deploys.sh` prunes old deploys keeping last N (default 3) + always protects `current`'s target. Followup `1abd387` removed all `$HOME/simkit_deploys` hardcoding — both scripts now auto-detect deploys-dir 2 levels up from their own location, falling back to `$SIMKIT_DEPLOYS_DIR` env or explicit arg. Live-verified end-to-end on home Linux as synthetic `workarea/simkit_deploys/`: first deploy via raw tar → deploy_venv smoke tests green → iteration via `current/scripts/unpack_payload.sh` auto-detect → cleanup `--keep 1` protects current + deletes old + leaves `*.tar.gz` files alone. Two bugs caught + fixed during testing: pipefail+tar+head SIGPIPE silent abort (use process substitution + `while read; break`); lock initially excluded setuptools+wheel breaking PEP 517 editable install (include them via `pip freeze --all` + `--no-build-isolation`). User's actual workflow: home Linux develops + pushes git → yellow Windows pulls + downloads wheels + bundles tarball → red Linux unpacks to `workarea/simkit_deploys/<sha>/` + deploys + activates via stable `current/` symlink. Daily activation: `cd <DEPLOYS>/current && source .venv/bin/activate`. DECISIONS #72.

**🎯 PHASE 3A — DONE 2026-05-19 (closeout).** Three-pillar architecture (Data / Define / Execute) all green: engineer authors `*.review.json` → `pvt run` drives Maestro through the battery → per-corner FAIL detected → 3 built-in strategies retry → results auto-ingested. Python **1153 / 0**, SKILL Tier-1 **462 / 0 / 0**. 19-day arc from v1 spec (2026-05-16) through v1.9 #4 (2026-05-19), 11 sub-versions, DECISIONS #50-71. Closeout fix landed in `skill/tests/runTests.il` — replaced Agent B's 4-layer self-locate fallback with the canonical `get_filename(piport)` primitive (idiom borrowed from sibling `skill_tools.il:102`); net `-37 LOC` (80 → 43). Live-verified from both `cwd=simkit` and `cwd=/tmp` — both produce 462/0/0, self-locate is cwd-independent. DECISIONS #71. v1.9 #4 remaining items deferred with explicit "wait for X" triggers in TODO.md: probe-oddity (benign so far), orchestrator opt-in to per-test pre-run scripts (data shape ready, needs real driver), dialog fixture race-proofing (wait for second occurrence). **No active phase — observing real usage before picking the next.**

**Phase 3A v1.9 #3 followup — corrected Tier-1 baseline + `'unbound` sentinel fix — DONE 2026-05-19 (same day as #69).** Bridge live re-verify of #69's "2 pre-existing SKILL Tier-1 FAILs" claim caught it was wrong twice over: (a) the count itself was a `LiteralRemoteFunction` readback proxy mis-read — `ws['_pvtTestPass']` returns a callable proxy, not the variable value; must use `ws['evalstring']('_pvtTestPass')`. (b) The `corners/collectRowNames/skips-missing-row_name` FAIL was a real (small) v1.9 #1 regression — its fixture `(makeTable 'rN 'unbound)` collided with SKILL's literal "variable unbound" sentinel, so `arrayref`-into-`let` triggered `*Error* eval: unbound variable: v`. Fixed in `skill/tests/testPvtCorners.il` (7× `'unbound` → `PVT_JSON_ABSENT`, +5 line section comment). The other FAIL (dialog fixture race) is a `/tmp/dialog_*` cleanup issue, not a code bug — `rm -rf /tmp/dialog_*` pre-run clears it. Tier-1 verified: **462 / 0 / 0** clean. New memory `[[skillbridge-readback-via-evalstring]]` records the proxy trap; `[[reference-tier1-baseline]]` updated with current baseline + correct readback recipe + +1/-1 environmental factor table. DECISIONS #70.

**Phase 3A v1.9 #3 additionalArgs snap/restore + 4 closeout items — DONE 2026-05-19.** PM-mode batch via two parallel subagents (Python deliverables + SKILL tooling). Closes v1.3 known-gap #1 (Gap #1 — both orchestrator's `_execute_ic_chained_item` and `TransPssIc.apply` now snapshot prior `additionalArgs` via v1.9 #2's probe primitive and restore per-test in `finally`, replacing the v1.3 unconditional clear-to-`""`). Lands per-test pre-run script latent infra (Gap #2 — `PreRunSpec.per_test_corner_to_arg` field + helper; orchestrator stays on shared-map). Pins 3-item chain semantics offline (Gap #3 — 7-case synthetic A→B→C pytest). Adds `SIMKIT_TRACE=1` env-gated tracer to all 3 strategies (default OFF). Closes sys.modules-None mock audit (5 greps, zero hits). Plus runTests.il atomic loader fix: 4-layer fallback (`_PVT_FORCE_ROOT` → env → upward cwd walk → cwd default) lets the script load from arbitrary cwd; empty-string env-var bug fixed. Live-verified Gap #1 on `fnxSession0` (byte-identical round-trip for both `Test` and `Test_trans`); SKILL Tier-1 460/2 from both cwds (2 FAILs pre-existing, queued as v1.9 #4). Python 1115 → 1153 (+38). DECISIONS #69.

**Phase 3A v1.9 #2 auto-probe `baseline_value` — DONE 2026-05-18 EOD.** Closes DECISIONS #63 D4 deferral. gmin_bump now probes the live asi for the option's current value at apply-start instead of using a hardcoded `"1e-12"` baseline. New SKILL `pvtRunnerGetSimOptionVal(sess, testName, optionKey)` + Python `pvt_runner_get_sim_option_val(...) -> Optional[str]` wrapper. Resolution order: **explicit sidecar > live probe > DEFAULT_BASELINE fallback**, with source tag in the apply-summary notes. Back-compat with existing mock bridges preserved via `getattr` lookup. Live-verified on `fnxSession0` — direct probe returns correct values for gmin/reltol/absent-option/missing-test; gmin_bump.apply() with no sidecar lands `1e-12` from probe and source tag in notes; corner-enable round-trips byte-identical. Python 1103 → 1115 (+12); SKILL Tier-1 +8 cases registered. Side finding: stale `additionalArgs` on Test/Test_trans confirms v1.3 gap #1 is a real concern (queued). DECISIONS #68.

**Phase 3A v1.9 #1 `pvt corners push --replace` — DONE 2026-05-18 EOD.** Closes v1.2 #2 / DECISIONS #54 #8 (v1.1 dogfood ADD-semantics surprise). Opt-in `--replace` flag; default unchanged for back-compat. SKILL `pvtCornersPush` gains `?replace nil` keyword — enumerates live corners, drops names not in sidecar via `axlGetCorner` + `axlRemoveElement`, then pushes. Wipe-protection guard refuses `replace=t` with 0 valid rows (runs before dry-run short-circuit so `--dry-run --replace` still surfaces the accident). Live-verified end-to-end on `fnxSession0`: 7-step probe — baseline pull → ADD-push 4 extras → 6 rows ✓ → REPLACE-push same extras → 4 rows ✓ → empty + no-row_name refusal both clean ✓ → dry-run + replace = 0 mutation ✓ → baseline restored via --replace at exit ✓. Python 1097 → 1103 (+6); SKILL Tier-1 +6 cases registered. DECISIONS #67.

**Phase 3A v1.8 #5 `trans_pss_ic` strategy — DONE 2026-05-18 EOD.** Second built-in intervention strategy on top of the v1.6 chain framework. Implemented as the **on-failure variant of v1.3 `ic_from`** (DECISIONS #57's pre-run-script mechanism reused verbatim — `readns/readic="<path>"` into per-corner `additionalArgs`). The asiChangeAnalysis probe deferred in DECISIONS #52 is now moot: #57 already provided a working IC-injection path. Sidecar params: `source_item` (required), `file`/`mode` (default `"ic"`/`"readic"`), optional `subdir` + `test_for_ic`. `StrategyContext` gains `history_by_item` + `pvtproject_path` optional fields so strategies can resolve upstream artefacts; orchestrator's `_run_strategy_chain` plumbs both through. Safe-write shape (`baseline_value=""`) inherits v1.7 A6 fix — partial-row FAIL sets don't leak across shared worker-VM asi. Live-verified on `fnxSession0`: FAIL=TT_pvt_3 → resolved 5th-explode-index → found `simkit_verify/5/Test/netlist/spectre.ic` → rendered script with correct readic injection → bridge call sequence + corner-enable round-trip + pre-run reattach all clean. Python 1069 → 1097 (+28). SKILL Tier-1 unchanged. DECISIONS #66.

**Phase 3A v1.8 #4 `pvt star` + Maestro history-lock sync — DONE 2026-05-18 EOD.** User-facing "starred run" concept with Maestro round-trip. Schema bumped v2 → v3 adding `runs.starred BOOLEAN DEFAULT FALSE`; bridge gets `pvt_runner_set_history_lock` (mae-name path) + `pvt_runner_get_history_lock_map` (axl-handle path); new CLI verbs `pvt star` / `pvt unstar` / `pvt sync-stars`; `pvt list` prepends ★ column and gains `--starred-only`. Sync semantics are direction-explicit (push = DB authoritative, pull = Maestro authoritative) with `--dry-run` for plan preview. Live-verified on `fnxSession0`: 5-step round-trip on `v17_gmin_demo__gmin2`, 3 pre-existing reference locks untouched. Python 1030 → 1069 (+39). DECISIONS #65.

**Phase 3A v1.8 #3 `pvt list` history column — DONE 2026-05-18 EOD (followup).** Verified the v1.5-reported "run.json `history_name: None`" before scheduling SKILL work. Write side and DB side were already correct since v1.5 PM; the report was a `dict.get()` misread. Real user-visible gap was the absence of a `history` column in `pvt list`'s default table — fixed in `cli/list_runs.py` (insert width-22 column between run_id and timestamp). 2 new tests pinning the column header presence and the ellipsis-truncation contract. Python 1028 → 1030. DECISIONS #64 records the phantom finding + the "verify before fixing" lesson.

**Phase 3A v1.7 `gmin_bump` strategy + worker-VM asi state-leak fix — DONE 2026-05-18 EOD.** First real intervention strategy on top of the v1.6 chain framework. `simkit.strategies.gmin_bump.GminBump` uses the v1.3 pre-run-script mechanism (DECISIONS #57) to inject `asiSetSimOptionVal asi "gmin" <bump>` per-corner inside the worker VM — bumped floor lands in that corner's netlist only, user's main-session GUI Spectre Options stays untouched. Default ramp `[1e-11, 1e-10, 1e-9]`, max_attempts=3, all overridable via sidecar `params`. A5 Phase 1 live-verify caught a state-leak bug — worker-VM asi sessions are **shared across sub-corners in the same sweep row** (NOT per-sub-corner-fresh as A1 assumed), so a partial-map override (which `gmin_bump` always is, unlike `ic_from` which maps every sub-corner) was carrying the bumped value across to unmapped corners. A6 fix: `PreRunSpec.baseline_value` field — when set, renderer emits a "baseline first, conditional override second" body that resolves asi unconditionally; ic_from's existing shape (`baseline_value=None` default) untouched so zero-regression for the v1.3 mechanism. A7 re-verify: same single-target probe POST-FIX, only TT_pvt_3 carries the 9.99e-10, TT_pvt_4/5 restored to 1e-12. Test additions: 30 cases in `test_strategies_gmin_bump.py` + 4 in `test_pre_run_script.py`. Python 996 → 1028 (+32). DECISIONS #63.

**Phase 3A v1.6 strategy chain dispatch — DONE 2026-05-18 PM late.** `execute()` now consumes `item.on_failure.strategies`. Per-item post-ingest flow: query DB FAIL corners (`simkit.failures`), filter eval_err out of retry targets, iterate strategy chain × `max_attempts`, naive_retry does corner-enable narrowing via sub→row longest-prefix map, snapshot/restore is in-finally so a botched retry doesn't leave the session mutated, retry artefacts append to `ItemResult.history_names` + `.run_dirs`, post-chain residue lands in `final_failed_corners`, CLI exit 6 honours it. Live-verified twice on fnxSession0 — eval_err surfaced + skipped (phase 1), forced retry drove the full snapshot/run/ingest/re-query path (phase 2). Two production bugs caught during dogfood (path shape + envelope shape) + mock fixtures updated. Closes v1.2 #3. Unblocks `gmin_bump` (now DONE per above) / `trans_pss_ic` to drop in as isolated strategy classes. DECISIONS #62.

**Phase 3A v1.3 cross-item IC piping (ic_from) — DONE 2026-05-17, 5 commits ahead of origin/main not pushed.** Architecture: schema v2 adds `ic_from: {item, file, mode}` on a consumer item; orchestrator's `_execute_ic_chained_item` walks the consumer's union explode order, resolves each sub-corner's spectre.{fc,ic,dc} from the upstream item's results dir, generates a self-contained SKILL pre-run script with the corner→arg map embedded as a `(list "name" "readns=\"path\"")` assoc, attaches it via `axlImportPreRunScript`, fires ONE `axlRunAllTests`. Maestro fires the pre-run script per (test, sub-corner) before each point's netlist gen; script looks up current sub-corner via `axlGetCornerNameForCurrentPointInRun` and writes the matching value into `additionalArgs` via `asiSetSimOptionVal` on the per-test asi session. Result: ONE Maestro history with all N sub-corners, per-corner IC delivered. Live-verified end-to-end on `fnxSession0` (Interactive.2: 3 per-corner subdirs, distinct IC paths per corner, Spectre ran clean). Final retry of readns="<path>" syntax pending (bridge session-focus dance interrupted; fix is purely textual and architecturally proven).

**Phase 3A: Simulation Orchestrator — §1-§5 + §6 (partial) DONE 2026-05-16 (v1).** v1 end-to-end smoke worked on fnxSession0: snapshot test state → enable-only → axlRunAllTests → axlSetHistoryName rename → PvtSave dump → `pvt ingest` → `pvt list` round-trip green. New result row (`c955d584-…`) sits alongside Phase 3B v1.3/v1.4 dogfood runs. Python suite 835 / 0 (was 797 + 38 net). SKILL Tier-1 unchanged (no Tier-1 added yet for pvtRunner — added in v1.1).

**S2 dogfood EXPOSED the v1 misnomer:** `axlRunAllTests` is fire-and-forget async, not sync — S2's "real-Spectre wall-clock" measurement was never captured because PvtSave dumped while sims were still queued ("pending" status across 4 corners). Cleanup of the orch_s2 history triggered Maestro's `ASSEMBLER-2423 setupdb temporarily locked` modal that wedged the bridge. Lesson pinned in DECISIONS #54 + memory `feedback_axl_run_async_wait_for_idle.md`: poll `axlGetRunStatus` to idle before any destructive op; check for modal dialogs FIRST when bridge looks wedged.

**Phase 3A: Simulation Orchestrator — §1 SPEC DONE 2026-05-16; §2 NEXT.** Spec at `docs/phase3a_orchestrator_spec.md`. Sidecar shape `*.review.json` is a named ordered list of items, each pairing **its own** (tests, union, bundle) — matches the user's real signoff workflow (BT2GRX trans PVT / BT2GRX PSS PN / LE trans / LE PSS / 干扰仿真 / … as ~10 named items). Failure handling = per-corner skip (item continues with remaining corners); items execute sequentially, intra-item concurrency delegated to Maestro/LSF. Strategy plugin architecture is in v1; gmin_bump + trans_pss_ic deferred to v1.1 (need separate asi* probe). v1 ships `naive_retry` placeholder as the only built-in. Phase 1 collector (`pvtCollIterateResults`) gets reused for per-corner pass/fail extraction.

**Phase 3B: Formula-Template Authoring — SKELETON + v1.1 BUILTINS + v1.2 EXPRESSIVENESS + v1.3 SPECS + v1.4 SPEC-CAPTURE-ROUND-TRIP + v1.5 #2-#4 DONE.** Skeleton landed earlier on 2026-05-14 via three parallel subagents (Python core, SKILL bridge, CLI); user is now able to author formula templates, declare signal groups, bundle template×group×test, and `pvt measure apply` to a live Maestro session. v1.1 (same-day, after the skeleton stabilised) added a 17-template builtins library reverse-engineered from one of the user's real production Outputs CSVs (130-row sim_DCOBUF testbench): every recurring shape — i_avg, freq, duty_cycle, rise/fall (auto + fixed-rail), DFT, edge_delay, IQ wrap, value_at — installable into any project with one `pvt measure install-builtins` command. The DCOBUF walkthrough fixture proves 20 hand-written CSV rows collapse to a 4-line measure bundle that renders byte-for-byte equal to the source.

Eight design decisions (P3B.1–5b + F1–F3) recorded in DECISIONS #38–#41; restore safety in #42; v1.1 builtins shape (ANALYSIS-as-param + signal+string edge_delay + collision-as-loud-failure) in #43; v1.2 expressiveness picks in #44; v1.3 spec passthrough in #45; v1.4 spec capture + axlSKILL spec-API map + the `?min`/`?max`-vs-`minimize`/`maximize` semantic finding + X..Y parser in #46.

Goal of Phase 3B (one sentence): "describe one formula in a sidecar template" → "apply to a named signal group on a named test" → "the rendered Outputs land in Maestro, the snapshot round-trips bit-identical."

**v1 scope NOT shipped** (deferred to v2): pre-baked rise_time / dutyCycle / avg_current template library (user authors against scaffold); multi-signal templates; cross-project template sharing (user-home `~/.simkit/templates/`); snapshot match-back (reverse-engineer a snapshot into bundle + template parameters); specs / user-defined Output columns.

**Phase 2: PVT-Union Builder — DONE.** Spec frozen; loader + explode + offline CLI (explode/list/diff) shipped; SKILL `pvtCornersPull` + `pvtCornersPush` extended to capture enable + abs-path; Python skillbridge wrapper + `pvt corners pull/push/build` CLI all shipped + live-verified against `fnxSession0`. Acceptance gates U1/U2/U3/U4 all offline-pinned (22 cases in `test_acceptance_phase2.py`). Open Decision 8.3 (CSV format) resolved 2026-05-13 by reverse-engineering a live `Tools → Corners → Export` sample — emitter produces byte-identical output.

**Owed back from user**: one-shot GUI verification that `pvt corners build` output is re-importable via `Tools → Corners → Import` after Cadence restarts (the crash-recovery use case). Until that's done, the recovery flow is "logically correct" (byte-identical to a confirmed export format) but has not been physically demonstrated.

**Phase 1: Data Pillar MVP — COMPLETE.** Six sections shipped: spec, `.pvtproject` loaders (Python + SKILL + first-save dialog), collector SKILL with messy-data refactor + Bug A/B/C/D fixes + netlist Spectre fix, Python ingester + inline validator, full `pvt` CLI surface (`ingest`/`validate`/`attach`/`label`/`list`/`diff`), 4 acceptance gates pinned as regression. Open items (§2.2 dialog Tier-2 manual, screenshot v1.1, walker mock-rdb) all deferred per their DECISIONS entries.

## Goal of Phase 2 (one sentence)

End-to-end loop: "describe one semantic PVT in a sidecar" → "tool emits the exploded Maestro corner table" → "round-trip is bit-identical against the live session."

## Goal of Phase 1 (one sentence — historical reference)

End-to-end loop: "Maestro sim finishes" → "one command saves it" → "Python can query it and diff two slices."

## Recent timeline

- **2026-04-21 → 2026-04-22**: initial design conversation closed all architectural questions. See `DECISIONS.md` entries #1–#12.
- **2026-04-22**: project scaffold + git repo; Phase 1 scope locked in `TODO.md`.
- **2026-04-22**: §1 Specification complete (`docs/schema.md`, `config/pvtproject.example.json`).
- **2026-04-22**: Decision #13 — JSON over YAML/TOML for `.pvtproject`.
- **2026-04-22**: §2 item 1 done — Python loader (`python/simkit/project.py`, 30 unittest tests).
- **2026-05-08**: §2 item 2 done — SKILL loader (`skill/pvtError.il`, `pvtJson.il`, `pvtProject.il`, ~1800 lines + 19 fixtures + 76 tests, all passing). Drove tests via skillbridge. Surfaced 12 classic-SKILL idiom traps; see Decision #14.
- **2026-05-08**: §3 prep — JSON emitter + collector API probe committed (`c94c0d2`). pvtCollect.il scaffolded (974 lines) + Tier-1 unit tests (377 lines, 41 new tests; cumulative 168/0/0).
- **2026-05-10**: §3 first end-to-end on real Maestro (`sim_yusheng/Test/maestro:simkit_verify`, 7 tests / 49 outputs). **Tier-1 was green; live run silently produced 0 rows** until we localized 9 funobj-call bugs in `pvtCollect.il` via skillbridge probing. Post-fix: 42 ok rows, correct corner_vars, correct testbench_alias resolution, valid JSON envelope. See Decision #16 for the funobj/funcall rule and slot inventory.
- **2026-05-11**: §4 Python ingester (`python/simkit/{db,ingest,schema_sql,errors}.py` + `cli/`) and §3(d) Python validator (`python/simkit/validate.py`, 24 invariants + 2 warnings) landed. 138 tests passing (30 baseline + 108 new). Validator wired inline in ingester by default (Decision #17). Surfaced and resolved 5 decisions: #17 inline validator, #18 nullable `netlist_path`, #19 internal `simkit_meta`, #20 per-file ingest transactions, #21 dropped FK in DDL (DuckDB constraint-relaxation limitation). 42-row real-run fixture from 2026-05-10 checked into `tests/fixtures/runs/` as the integration anchor.
- **2026-05-11**: §3 messy-data approach (a) refactor landed end-to-end. `_pvtCollIterateResults` is now a 5-line composer over `_pvtCollWalkRdb` (live walk → walkData) + `_pvtCollRowsFromTuples` (pure shaper, owns all three passes). Tier-1 grew 167→215 (+16 tests / +48 assertions) covering all six TODO §3 scenarios + pre-fix Bug A/C/D documentation tests. Tier-2 verified via skillbridge against `simkit_verify` — byte-identical 42-row output (`firstTestName="Test"`, first row `Rtime_clkout=2.134521e-11` matches pre-refactor). Bug A/B/C/D fixes (`"running"`→`"unknown"`, gappy-pid walker B2, pass-2 writtenSet skip, unified `_no_corner_vars` marker) queued as Step 4.
- **2026-05-11 (overnight)**: §3 messy-data Step 4 landed — all four bugs fixed in separate commits, Tier-1 215/1/0 maintained, Tier-2 byte-identical regression on `simkit_verify` (42/42 data rows match 2026-05-10 reference). Pre-fix `CURRENTLY-…` tests flipped to assert post-fix behaviour. Two scope notes captured in DECISIONS #22 (corner_vars marker unified across all three passes, not just pass-3) and #23 (walker-level Tier-1 testing deferred — Bug B verified by Tier-2 happy-path regression + correct-by-reasoning, awaits real gappy-pid sim or future synthetic-rdb harness).
- **2026-05-12 (overnight)**: §5 `pvt` CLI fill-out landed in five commits — `pvt attach`, `pvt label`, `pvt list`, `pvt validate --from-db`, `pvt diff`. Pure-Python; full Tier-1 138 → 242 / 0 (104 new tests across 8 new test files). Surfaced three design decisions: #24 (diff slice resolution = exact-label-then-prefix), #25 (label re-policy = error w/o `--force`, `--clear` unconditional), #26 (DuckDB TIMESTAMPTZ ↔ Python via `CAST AS VARCHAR` + ISO normalisation, to avoid the stdlib-pytz dependency the offline-deploy constraint forbids). All §5 commands open the DB read-only when they only read.
- **2026-05-12 (morning)**: §3 netlist Spectre detection fixed (DECISIONS #27). Pre-fix probe used `asiGetAnalogSimulator` — wrong API for Maestro / ADE-XL contexts — and returned nil on every spectre run. Replaced with a path-presence test: `<netlistDir>/input.scs` exists iff the simulator is Spectre. Tier-2 verified on the live `simkit_verify` session: `netlist_path="input.scs"`, 2938-byte `input.scs` copied into the run dir, `pvt validate` clean (no W2), `pvt ingest` populates `runs.netlist_path` non-null. 42-row count + first-row value match the 2026-05-10 reference, so no other behaviour shifted. SKILL Tier-1: 224/1 unchanged.
- **2026-05-12 (morning)**: §6 end-to-end acceptance gates pinned. `tests/fixtures/acceptance/` carries the live-captured `run_a` (full simkit_verify dump from earlier today) plus a synthesised `run_b` (manual C0 capacitor edit + per-row +1% value delta) and a 69-byte dummy PNG. `tests/test_acceptance.py` exercises all four §6 gates — save→ingest→query, TT worst-case across the 7 corners, netlist diff between two slices, post-hoc attach + retrieve — as 12 unit tests that don't need live Maestro at test time. Phase 1 Python suite: 242 → 254 / 0.
- **2026-05-12 (afternoon)**: §2 item 3 — SKILL first-save dialog landed (`skill/pvtProjectDialog.il`, 482 lines). v1 scope per DECISIONS #28 is four fields: Project name (required), DB root / Author / Save path (optional with defaults). Validation-fail UX uses `?unmapAfterCB t` + `hiSetCallbackStatus` to keep the form open. Tier-1 +23 tests via skillbridge → cumulative 256 / 1 / 0 (the 1 fail is the unchanged Maestro-open no-session baseline). Three new classic-SKILL traps surfaced and added to DECISIONS #14's idiom list (now reaches #15): (13) `(procedure (name ()) ...)` is wrong for zero-arg; (14) `?okButtonText` is not a real `hiCreateAppForm` keyword; (15) `boundp` is false for procedures — caught during Tier-2 smoke when the step-3 gate in `pvtProject.il` never flipped despite the dialog file being loaded. Fixed in `pvtProject.il:337` (`boundp` → `getd`); pre-existing bug that no test had exercised. Tier-2 happy path visually confirmed on live Virtuoso: form rendered, OK click wrote a clean 190-byte `.pvtproject` (project from Maestro session lib name `sim_yusheng`, blank dbRoot defaulted, blank author got `$USER`, schema_version emitted as bare int). Smoke test (`/tmp/dialog_smoke.py`) also covers programmatic happy / validation-fail / `?allowDialog nil` paths — 11/11 checks pass.
- **2026-05-12 (evening)**: Phase 2 kicked off. §1 spec frozen at `docs/phase2_pvt_union_spec.md` + `config/pvt_union_example.union.json`. Data model recovered live via skillbridge against `fnxSession0` — discovered Maestro stores corner sweeps on two parallel axes (vars + models), each using the same space-separated-string sweep encoding. Initial single-axis draft was wrong; revised to vars + models.section (model file/block/test stay at defaults for v1). Explode order rule reverse-engineered from the live TT_pvt 6-sub-corner expansion: alphabetic key + lex-sorted values (DECISIONS #30). Six open decisions (8.1-8.6) flagged for resolution during §2; only 8.4 (axlSetParameter device-level overrides) is potentially scope-shifting. PM mode + verification gate granted by user: SKILL code must be skillbridge-verified non-blocking; Python must be runtime-verified.
- **2026-05-12 (overnight)**: Phase 2 §2 + §5 (offline subset) + §3 (code only) landed end-to-end in four commits. `d4d4ac0` §1 spec + DECISIONS #29-31 + housekeeping; `9d4ee6d` §2 Python loader + 41 tests (example file renamed to `pvt_union_example.union.json` to conform to spec §3.1 two-part extension); `e5f9a8f` §3 SKILL `pvtCornersPull` + 28 Tier-1 cases — code committed, verification pending; `9179fdf` §5 `pvt corners explode`/`list`/`diff` offline subcommands + 30 tests. Python suite: 254 → 325 (+71).
- **2026-05-12 (late evening — after user reload of sbStart.il)**: §3.V verification cleared. `a98b9e5` fixes 5 bugs caught during Tier-1 + Tier-2: 4 operator-shorthand uses (`<=` / `<` / `+` / `-` → `leqp` / `lessp` / `plus` / `difference`; DECISIONS #32) and 1 `pvtJsonEmitToPort` arg-order bug. SKILL Tier-1 256 → 286 / 1 (baseline FAIL unchanged); Tier-2 live `pvtCornersPull` against `fnxSession0` reproduces spec §9 verbatim. DECISIONS #33 records the verification.
- **2026-05-12 (late evening — after user restarted Cadence)**: Phase 2 §3 push side landed and verified. `263adb0` adds `pvtCornersPush` + 13 push-helper Tier-1 cases (cumulative SKILL 256 → 300 / 0 fail — 1 baseline FAIL flipped to PASS in the fresh VM; net +30 corner tests). Tier-2 pull→push→pull round-trip on `fnxSession0` (3 corners: TT scalar / TT_pvt with vars+models sweeps / TT_2p5G with 3 scalar vars) is **semantically byte-identical** modulo per-pull `name` field. DECISIONS #34 captures the verification.
- **2026-05-12 (closing)**: §6 Gate U3 pinned as 6-test pytest module (`tests/test_acceptance_phase2.py`); Python suite 325 → 331 / 0. U1 manually verified live (the Tier-2 round-trip in `263adb0`) but not yet offline-pinned (awaits captured fixture pair). U2 (VCO LO) and U4 (CSV round-trip) remain deferred — U2 needs VCO LO loaded, U4 needs `pvt corners build` to lock the CSV format (Open Decision 8.3). User pre-staged two history runs in `fnxSession0` for the Phase 1 §3 messy-data deferred Tier-2 verification: `simkit_simerr` (all-sim-err) and `simkit_Rtime_err` (one corner Rtime_clkout eval-err). Cataloged in TODO §1 Backlog.
- **2026-05-12 (overnight)**: Phase 1 §3 messy-data Tier-2 closed via the new histories. `d150990` introduces `eval_err` per-output sentinel (DECISIONS #35): pass-1 now emits a row preserving the output name for any value the rdb returns that isn't number/non-"wave"-string — fixed the silent drop of TT_2p5G/Rtime_clkout in `simkit_Rtime_err`. Validator I12/I14/I1 extended. SKILL Tier-1 300 → 313 / 1 baseline; Python 331 → 338 / 0. `simkit_simerr` already worked cleanly (pass-2 only).
- **2026-05-13 (morning)**: Phase 2 §6 Gate U2 closed via synthesised 21×3 VCO shape (`92ab6f5`). User didn't have actual VCO LO loaded; I built the shape from the PHASE_PLAN.md description (7 process × 3 ind-temp = 21 rows; each row sweeps temperature ×3 = 63 sub-corners), pushed into `fnxSession0` (3 → 24 rows), pulled back byte-identical for all 21 rows. Fixture pinned at `tests/fixtures/unions/vco_lo_21x3.union.json` + 5 pytest cases. Python 338 → 343 / 0. DECISIONS #36 records rationale and the open clause on 8.6 (multi-axis-per-row at scale still untested). Surfaced a UX caveat: `["-40","25","105"]` lex-sorts to `["-40","105","25"]` (numeric-string gotcha already noted in spec §3.4); flagged to user.
- **2026-05-13 (cleanup)**: 21 synthesised VCO corners removed from `fnxSession0` via `axlRemoveElement`. Session is back to its original 3 rows (TT / TT_pvt / TT_2p5G). No commit — cleanup is an interactive operation, not a code change. Bridge state had to be reset twice during the session (`pyKillServer` / `pyStartServer` recovery procedure documented in user-memory `reference_skillbridge_recovery.md`).
- **2026-05-13 (mid-day)**: Gate U1 offline-pinned (`8ae37bf`). Live capture via `/tmp/u1_capture_edit.py` ran the full round trip on `fnxSession0`: pull baseline (3 rows, TT.temperature=55) → programmatic edit (TT.temperature=85) → push edited → pull post_edit_pull → assert byte-identical modulo top-level `name` → push baseline back → assert `fnxSession0` restored. Three fixtures + 6 pytest cases pinned in `tests/fixtures/unions/u1_*` + `TestGateU1RoundTrip`. Python 343 → 349 / 0.
- **2026-05-13 (afternoon)**: Phase 2 §5 push/pull CLI wrappers shipped (`6ef6cea`). New `python/simkit/skill_bridge.py` exposes `pvt_corners_pull` / `pvt_corners_push` as Python callables — re-loads production .il files per invocation (idempotent, <1s), cwds to `.pvtproject` parent before each call, decodes `pvt_ok` / `pvt_err` into `SkillBridgeError`. CLI: `pvt corners pull <out.union.json>` and `pvt corners push <union.json> [--dry-run]` with `--project` / `--session` overrides. 28 new Tier-1 cases (18 wrapper + 10 CLI via mocked `Workspace`). Runtime-verified against live `fnxSession0` via `/tmp/cli_live_verify.sh`: pull → push back → pull → diff shows 3/3 identical; dry-run does not perturb live state. Python suite 349 → 377 / 0.
- **2026-05-14 (later same day, after skeleton stabilised)**: Phase 3B v1.1 builtins library shipped. User provided one of their real production Outputs CSVs (130-row sim_DCOBUF DCO-buffer characterisation testbench); reverse-engineered the recurring measurement shapes into 17 reusable templates with `ANALYSIS` parameterised as a string param (default `tran`; overridable to `pss` / `dc` / `ac` per bundle entry) so one template body serves every analysis type. Edge-delay templates use `1 signal + 1 string ref` (SIG_A iterates, SIG_B fixed via param_overrides) — keeps within P3B v1's "≤1 signal-kind param" rule while still expressing N-vs-1 delay-from-reference, the dominant real-world shape. New `pvt measure install-builtins [--force] [--names …] [--list]` CLI subcommand copies the library into a project's `templatesDir/`. Walkthrough fixture in `tests/fixtures/builtins_walkthrough/` collapses 20 hand-written DCOBUF CSV rows (5 clock nets × {Freq, DutyC, Rtime, Ftime}) into a 4-entry `.measure.json` that renders byte-for-byte equal to the source. Also pinned the known v1 collision behaviour: a signal_group whose nets share basenames (e.g. four /VDD supplies) surfaces as `RenderError` — needs a v2 per-signal alias map. Python suite: 598 → 602 / 0 (+17 new cases across `test_builtins.py`, `test_cli_measure.py::InstallBuiltinsCliTests`, `test_builtins_walkthrough.py`). DECISIONS #43 captures the three shape choices + the inventory + the collision-as-loud-failure pin.
- **2026-05-14**: Phase 3B skeleton shipped end-to-end across one session. (a) §1 spec frozen at `docs/phase3b_measure_template_spec.md`; DECISIONS #38–#41 record the eight P3B decisions + Outputs API map + tri-sidecar architecture. (b) Live skillbridge probe against `fnxSession0` mapped the API surface (`axlAddOutputExpr` / `axlOutputsExportToFile` / `axlOutputsImportFromFile` / `axlDeleteOutput`). No Maestro-side parse-without-evaluate entry exists → F3 syntax check moved to Python. (c) Three subagents in parallel: A shipped python/simkit/{template,signal_group,measure_bundle,template_render,template_paste}.py (5 modules + 106 tests; Gate M1 + M4 pinned). B shipped skill/pvtMeasure.il (685 lines, +47 Tier-1 cases, 0.04s live probe on fnxSession0). C shipped python/simkit/cli/measure.py (12 subcommands) + skill_bridge.py extension (+272 lines, pvt_measure_push/pull/restore wrappers + 80 tests). (d) Paste-importer quote-preservation fix landed (`"/Vout"` → `"$SIG"` keeps surrounding quotes so render reconstitutes the source byte-for-byte). (e) Live M2 + M3 verification on fnxSession0 caught a sharp edge: default `pull --no-signals` + default `restore overwrite` wipes signal-tap rows; resolved by changing `restore` default to `merge` (DECISIONS #42). Final tally: Python 421 → 607 (+186); SKILL Tier-1 300/5 → 347/5; fnxSession0 returned to clean 11-row baseline after every verification cycle.
- **2026-05-16 (last, post-v1 dogfood)**: Phase 3A v1.1 #1 (DECISIONS #55) + DECISIONS #56 cwd-leak fix landed in one session. **v1.1 #1**: SKILL `pvtRunnerRun` split into `pvtRunnerSubmit` + `pvtRunnerRename` (+ legacy `pvtRunnerRun` thin-wrapper for CIW back-compat); Python `pvt_runner_run` rewritten as Submit → poll-to-idle state machine (kwargs `poll_interval`/`timeout_sec`/`idle_confirm_reads`/`dispatch_grace_reads`/`initial_wait_sec`/`post_idle_quiesce_sec`) → Rename; `pvt_runner_get_status` gains handle-0 RuntimeError→`(0,0)` translation; Tier-1 +13 (validation paths only — pure-call cases) + 9 Python state-machine cases (cached, real-run, handle-0, timeout, idle-confirm-consecutive, empty-name pre-submit, get_status isolation). Orchestrator `execute()` gains `run_kwargs` passthrough. **v1.1 #1 live dogfood (first round, fnxSession0)**: cached path 3.4s clean; real-Spectre path returned [0,0] in 6.8s then ASSEMBLER-2423 on cleanup → DECISIONS #55 misdiagnosed as "axlGetRunStatus is blind on this session"; pinned `initial_wait_sec` / `post_idle_quiesce_sec` as user-tuned escape hatches. **DECISIONS #56 follow-up**: parallel-agent investigation via `runICRP21` inspection + `axlGetSessionLibName`/`ddGetObj` cross-check found root cause was `_prep`'s `changeWorkingDir(simkit_p3b_dogfood)` leaking into the next `axlRunAllTests`, so the AXL worker booted in a dir without `cds.lib` (Spectre never dispatched). Fix: convert `_prep` + `_prep_measure` to `@contextmanager`s; wrap `pvt_save` body in `_restore_cwd(ws)`; mock workspaces in both test files now stub `getWorkingDir`; 4 existing cwd-pinning tests updated to assert [enter, restore] 2-call contract; +3 new tests (`pvt_save` happy + `pvt_save` restore-on-error + `pvt_corners_pull` restore-on-error). **Re-verify on fnxSession0**: Job27.log now in `Test/workarea/logs_yusheng/logs0/` (was `simkit_p3b_dogfood/logs_*/` pre-fix), worker boots clean, Spectre actually dispatches (per-corner netlist dir materialises, Spectre runs far enough to emit its own SFE-73 error on the synthetic test corner — unrelated to simkit). DECISIONS #55 retracted in-file: `axlGetRunStatus` returning `[0,0]` was correct (no run in flight); the symptom was Spectre never starting. **Remaining v1.2 ticket**: Maestro post-Spectre flag-stickiness (cleanup ASSEMBLER-2423 fires even after Spectre exits because Maestro keeps the history "actively running" flag set until the worker formally finishes). Python 835 → 871 (+36). SKILL Tier-1 394/1 → 407/1 (+13). 4 orphan histories on fnxSession0 owed back to user via GUI delete: `Interactive.0`, `Interactive.1`, `orch_v11_real_1778936686`, `orch_v11_real_1778939316`. Corner table at 3-row baseline.

- **2026-05-16 (late, after Phase 3B close)**: Phase 3A kicked off. §1 spec frozen at `docs/phase3a_orchestrator_spec.md` (220 lines, §1-10); example sidecar at `config/review_example.review.json` (5-item BT2GRX-style); DECISIONS #50-53 land (#50 review.json shape, #51 per-corner skip + sequential items, #52 strategy plugin + v1 scope = framework + naive_retry only, #53 SKILL API map). Live skillbridge probe against `fnxSession0` (sim_yusheng/Test/maestro/config) mapped the `axl*` run-control surface end-to-end (axlGetMainSetupDB / axlGetEnabledTests / axlGetTest / axlSetEnabled / axlRunAllTests + axlRunAllTestsWithCallback / axlGetRunStatus / axlGetHistory / axlStop family / axlGetResultsLocation) and surfaced the `asi*` simulator-interface namespace (298 functions; `asiAddSimOption` for gmin, `asiChangeAnalysis` for PSS IC — both deferred to v1.1 with own probe). Two gotchas surfaced + memory-pinned: (a) `axlGetWindowSession()` from CIW returns nil even with Maestro open → workaround = always pass session name explicitly (DECISIONS #53); (b) bare `(pyStartServer)` fails with code 127 because `python` binary doesn't exist on this host → fix `(pyStartServer ?python "/usr/bin/python3")` or load `sbStart.il` (updated `reference_skillbridge_recovery.md` memory). User chose per-corner skip granularity + v1 strategy scope = framework only. TODO.md Phase 3A section added with §1-§6 breakdown + v1.1 deferred list. No code touched yet; §2-§5 next.

- **2026-05-16 (later still)**: Phase 3B v1.5 #3 + #4 — (#3, DECISIONS #48) per-iteration `specs: [str|null, ...]` parallel array on swept `MeasureApply` entries; mutex with the single uniform `spec`; renderer reuses the existing per-row `RenderedRow.spec` plumbing so SKILL push is untouched. Motivating case: PN spot-frequency sweeps where `PN @ 1MHz < -100` and `PN @ 100MHz < -140` need distinct specs in one entry. (#4, DECISIONS #49) `signal_group_schema_version` accepts `{1, 2}`; v2 items can be `{net, alias}` objects, alias replaces basename in rendered output names — natively absorbs the dco2g_supplies /VDD×4 basename-collision idiom from the v1.1 walkthrough. `SignalGroup.signals` is now `tuple[Signal, ...]` (was `tuple[str, ...]`); 2 production renderer callsites + 3 test files updated; v1 bare-string sidecars unaffected. Walkthrough fixture left in v1 form as the regression pin for the collision case; new `SignalAliasRenderTests` carries the v2-form happy path. Pure-Python change — no SKILL touched, no live verification needed (net paths reach SKILL identical; only Python output_name strings differ, which SKILL has always treated opaquely). Python 773 → 797 (+24, broken down: #3 +11, #4 +13). SKILL Tier-1 394/1 unchanged.
- **2026-05-16 (later)**: Phase 3B v1.5 #2 fix — inclusive `>=` / `<=` bundle specs no longer dispatch to `?min` / `?max` (which Maestro stores as `minimize X` / `maximize X` = ADE-XL target-style, pass = value ≤ X / ≥ X — semantic opposite of inclusive bound). axlSKILL has no `?ge` / `?le` keyword, so the dispatch now uses `?range` with a sentinel: `("ge" v)` → `?range (v 1e30)`, `("le" v)` → `?range (-1e30 v)` (`_PVT_MEASURE_SPEC_HUGE = 1.0e30`). SKILL parser tags renamed `min` → `ge`, `max` → `le` to match Python `spec_eval` convention. Cadence storage: `range V 1e+30` / `range -1e+30 V`; `spec_eval.parse_spec` already handles these as inclusive intervals. Live-verified on fnxSession0: `>= 1e-10` on Rtime_clkout → CSV shows `range 1e-10 1e+30`; `<= -100` on PN_1M → `range -1e+30 -100`; `evaluate_spec` classifies 2.13e-11 (fail) / -167 (pass) correctly. Cleanup via overwrite-import recipe (#46 G); fnxSession0 back to 11-row baseline. SKILL Tier-1 394/1 (2 assertions retagged). DECISIONS #47.
- **2026-05-16**: Phase 3B v1.4 spec-capture round-trip shipped — closed the gap the 2026-05-16 morning dogfood surfaced (collector predated specs, result rows had no pass/fail field). Four-layer touch in one push: (1) SKILL pvtCollect.il gains `_pvtCollCaptureSpecs(sess)` that pulls per-(test, output) spec strings via `axlOutputsExportToFile` and attaches them as a top-level `output_specs` map; envelope schema_version 1 → 2. (2) New Python module `simkit.spec_eval` parses Cadence-native strings (both user-friendly bundle forms AND Maestro CSV-normalised forms: `< X`, `> X`, `<=X`, `>=X`, `minimize X`, `maximize X`, `range X Y`, `[X:Y]`, `X..Y`, `tolerance X` (parsed but unsupported eval)) and produces a 6-valued verdict enum (pass/fail/no_spec/no_value/unsupported/parse_err). (3) DuckDB schema bumped 1 → 2 with additive `results.spec` + `results.spec_status` columns and a one-step ALTER migration; ingester denormalises spec + verdict per row. v1 envelopes ingest as spec=NULL, spec_status=NULL (distinct from v2's 'no_spec' = "checked, no spec"). (4) `pvt list` adds a "specs" column showing `<pass>/<has_spec>` with FAIL marker, plus `--failed-only` filter; `pvt diff --spec-changes` filters to verdict-flipped rows with spec_a/spec_b columns. Two bonus side-fixes from the same investigation: #4 X..Y dotted range form in the SKILL spec parser (separate commit), #5 spec_status surfaced in apply CLI summary (separate commit). #3 (weight pull) scope-shrunk after live probe found weight IS readable but the `?info` field has no read accessor and user confirmed they never adjust weight in practice — deferred to v1.5. DECISIONS #46 captures the axlSKILL spec-API map (`axlGetSpecs(sdb)` → count + address list; `axlGetSpec(sdb, "<test>.<output>")` → integer handle via FULL address; `axlGetSpecName / ResultName / Type / Min / Max / Tol / Weight` per-handle) AND the latent v1.3 round-trip bug where `?min`/`?max` (which Maestro stores as `minimize`/`maximize` = target-style, NOT inclusive bounds) are semantically opposite of what the v1.3 mapping from `>=X`/`<=X` assumed. Python 678 → 773 (+95); SKILL Tier-1 376 → 394 (+18); baseline FAIL unchanged. Live dogfood on `fnxSession0` + `simkit_check_spec` history: PvtSave → ingest → query showed Rtime_clkout=2.13e-11 → pass (`< 1e-10`), PN_1M=-167 → fail (`> -150`), old v1.3 dogfood run still NULL-NULL. fnxSession0 left with 2 specs (no axlDel API).
- **2026-05-15**: Phase 3B v1.3 spec passthrough shipped — closed the gap where the framework pulled Maestro's Spec column but discarded it on push. User asked: "你的这些公式好像完全没有提及spec，但是cadence的公式里面是支持像是< > range这些spec判断的". Right — the snapshot.json schema had `spec` since v1.0 but render/apply/SKILL push never set it. v1.3 adds an optional `spec: str` field on apply entries (template / raw / sweep), passed Cadence-native (`<100p` / `range -150 -100` / `[2.4G:2.6G]`). SKILL push parses via `_pvtMeasureSafeEvalNumber` (manual char-set check + evalstring — necessary because errset cannot catch evalstring's unbound-var error path, verified live) and dispatches to `axlAddSpecToOutput` with the appropriate exclusive keyword. Cadence normalises on store: `<100p` becomes `< 1e-10` after push+pull (semantic round-trip, not byte-identical). Per-row report gains a `spec_status` field separate from the primary added/replaced status — spec failure does NOT abort the batch. Python 662 → 678 / 0 (+16). SKILL Tier-1 347 → 376 / 1 (+29 cases). Live dogfood `dogfood_v3.measure.json` confirms PN_* with `<-100` and Rtime_clkout with `<100p` round-trip semantically. fnxSession0 restored to baseline after. DECISIONS #45.
- **2026-05-15**: Phase 3B v1.2 expressiveness pass shipped. Dogfood against live `fnxSession0` (11-row baseline: 4 nets + 7 expr — `Rtime_clkout`, `PN_wave`, 5× `PN_*`) surfaced 6 friction items: (a) output_name override, (b) rise/fall need an unclipped variant, (c) signal_group:null should be implicit when meaningless, (d) list-bundles STATUS column was truncating useful errors, (e) the 5 PN_* spot frequencies had to be 5 hand-written bundle entries, (f) `PN_wave`'s composite `rfEdgePhaseNoise(...)` had no builtin shape and no escape hatch. All six closed in one push: `measure_schema_version` bumped to 2 (v1 bundles still load; v2-only fields gated); 4 new `_full` builtins follow the `i_avg_window`/`i_avg_full` precedent instead of introducing a CLIP parameter (user pushed back on the parameter approach for being inconsistent with existing convention); new MeasureApply fields `output_name`, `param_sweep`, `output_names`, plus a peer `raw_expression` entry kind. Dogfood proof: a 3-entry `dogfood_v2.measure.json` (1 raw + 1 sweep + 1 template-with-override) describes all 7 fnxSession0 expr rows byte-identical; `pvt measure apply --replace` → pull → diff vs. pre-apply baseline.snapshot.json is 11/11 clean. Python suite 602 → 662 / 0 (+60). SKILL untouched. DECISIONS #44 captures the scope, the picks, and the naming-vs-parameter rule. Dogfood workspace lives at `~/cadence_work/simkit_p3b_dogfood/`.
- **2026-05-13 (evening)**: Phase 2 §5 `pvt corners restore` shipped (`e641713`). User asked whether SKILL could import CSV directly (no GUI dependency for verification). No documented `axl*ImportCornersFromCSV` exists — only `axlLoadCorners`(.sdb) and `axlLoadCornersFromPcfToSetupDB`(.pcf). But since `pvtCornersPush` already drives `axlPutCorner`/`axlPutVar`/`axlPutModel` directly, the equivalent path is: parse CSV → Union → existing push. `parse_csv` is the reverse of `build_csv`; CLI `pvt corners restore <csv>` ties it to push with auto-resolved testbench_id (via new `resolve_live_testbench_id` helper that walks `axlGetSession{Lib,Cell,View}Name`). Also fixed `_prep` to pin `PVT_PROJECT` inside Virtuoso to the resolved project (stale env var from prior probes was causing pulls to fail-fast). End-to-end live verified against `fnxSession0`: pull → build → restore → pull → diff is 3/3 identical. Python 411 → 421 / 0. **Note**: this is skillbridge-import for convenience; GUI Import remains the truly-decoupled fallback for Cadence-crash recovery.
- **2026-05-13 (late afternoon)**: Phase 2 §5 `pvt corners build` + Gate U4 landed (`538b0df`). User pushed back on CSV scope — "正常使用不会用，但要作为 Cadence 闪退的备份保护" — so I extended SKILL pull to capture per-row `enabled` (via `axlGetEnabled`) and per-model `_file_abs` (via `axlGetModelFile`), extended Python `Union`/`UnionRow`/`ModelEntry` schema with `enabled: bool = True` and `file_abs: str | None = None` (backward-compat defaults), and wrote `python/simkit/corners_csv.py` to emit Maestro-format CSV. Ground truth at `tests/fixtures/unions/fnxsession0_baseline.csv` from a live `Tools → Corners → Export` on `fnxSession0`; emitter produces **byte-identical** output (Gate U4 pinned in 5-case `TestGateU4SidecarToCSV`). Discovery: Maestro displays `temperature` as `Temperature` in the GUI/CSV (case-rule in emitter). `block`/`test` columns in CSV are testbench cell name, not SKILL-side defaults `"Global"/"All"`. Open Decision 8.3 resolved. Python 377 → 411 / 0 (+34). One physical action still owed by user: verify the emitted CSV is re-importable via `Tools → Corners → Import` on a fresh Maestro (the actual crash-recovery flow).

## What's DONE

- All architectural decisions for the data pillar (see `DECISIONS.md`)
- Phase 1 scope defined (see `TODO.md`)
- Project scaffold and git repo
- §1 Specification: schema spec + example `.pvtproject`
- §2 item 1 — Python `.pvtproject` loader
- §2 item 2 — SKILL `.pvtproject` loader (commit `a3c8651`)
- §3 collector core — `PvtSave` entry, auto-capture, results iteration, JSON write, netlist copy (Spectre detection via file presence; DECISIONS #27). Verified end-to-end on a real Maestro history; 2026-05-12 Tier-2 run on `simkit_verify` confirms netlist_path now populates correctly. Tier-1 224/1/0 (1 baseline FAIL: no-session test with Maestro open).
- §3 messy-data refactor (a) Steps 1–4 — walker/shaper split + four targeted bugfixes (Bug A non-symbol status → `"unknown"`; Bug B walker pidList from `tst->pointID`; Bug C pass-2 per-test writtenSet skip; Bug D unified `_no_corner_vars` marker across all passes). Tier-2 byte-identical 42-row regression on `simkit_verify` confirms zero happy-path regression.
- §4 Python ingester — scan dump dir → DuckDB; idempotent on `run_id`; `schema_version` dispatch; inline-by-default validator. CLI: `pvt ingest` and `pvt validate`. 108 new tests, 138 total green.
- §3(d) Python validator — 24 invariants + 2 warnings (`W1` corner_vars magic markers, `W2` null netlist_path). Independently invocable (`pvt validate <path>`) and inlined in `ingest_run_json` by default. `pvt validate --from-db <run_id>` audits a DB-resident run by reconstructing the JSON-dump shape (DECISIONS #26).
- §5 `pvt` CLI surface — `attach`, `label` (set / `--force` / `--clear`), `list` (table or `--json`, `--slice-only`, `--project`, `--limit`), `diff` (results table + unified netlist diff, `--threshold`, `--include-status`, `--json`). Tier-1 grew 138 → 242 / 0; 8 new test modules. Slice resolution rule per DECISIONS #24.
- §6 end-to-end acceptance — 4 gates pinned as 12 tests in `tests/test_acceptance.py` against `tests/fixtures/acceptance/` (live `simkit_verify` dump + synthesised variant with documented manual netlist edit + dummy PNG). Demonstrates the full save→ingest→query→diff→attach loop without needing live Maestro at test time.
- §2 item 3 — SKILL first-save dialog (`skill/pvtProjectDialog.il` + `skill/tests/testPvtProjectDialog.il`). v1 four-field scope per DECISIONS #28. Tier-1 256 / 1 / 0 (the 1 baseline FAIL unchanged). Production-side classic-SKILL idioms validated against `virtuoso-skill` PDF index for every `hi*`/`axl*` call. **Tier-2 manual UI verification still owed** — 5 scenarios in `skill/tests/tier2/scenarios.md`, sandbox at `/home/yusheng/cadence_work/dialog_sandbox/`.

### Phase 2 — done

- §1 spec frozen (`docs/phase2_pvt_union_spec.md` + `config/pvt_union_example.union.json`). DECISIONS #29-31.
- §2 Python loader + explode (`python/simkit/union.py`, 41 tests).
- §3 SKILL `pvtCornersPull` + `pvtCornersPush` (`skill/pvtCorners.il`); Tier-1 +43 cases, Tier-2 live-verified against `fnxSession0`. DECISIONS #32-34.
- §5 offline CLI (`pvt corners explode / list / diff`, 30 tests).
- §5 live CLI (`pvt corners pull / push` via `python/simkit/skill_bridge.py`, 28 tests + live-verified against `fnxSession0`).
- §6 Gate U1 offline-pinned (pre/post fixture triple + 6 cases).
- §6 Gate U2 closed via 21×3 synthesised VCO LO push/pull round-trip + 5 cases.
- §6 Gate U3 closed via 2×3×5 = 30 synthetic explode + 6 cases.
- §5 `pvt corners build` shipped + Gate U4 byte-identical pin (5 cases). Live `Tools → Corners → Export` ground truth at `tests/fixtures/unions/fnxsession0_baseline.csv`; emitter at `python/simkit/corners_csv.py`.
- Pull extended to capture per-row `enabled` + per-model `_file_abs`. Backward-compatible defaults preserve old sidecars.

## What's IN PROGRESS

**Phase 3A v1.3 ic_from — DONE, final retry of readns="<path>" form pending.** Mechanism end-to-end proven via Interactive.2 dogfood on `fnxSession0` (3 per-corner subdirs in ONE history, each `input.scs` carries distinct per-corner IC path, Spectre completed successfully). Final live retry of the corrected `readns="<path>"` syntax (commit `efb4cac`) didn't complete because session-focus dance wedged the bridge mid-loop. High confidence the fix works: it's the EXACT syntax engineer types manually into Spectre Options form, and the textual change is purely in `build_corner_arg_map` — mechanism unchanged. **Next-session dogfood task = retry that single readns="<path>" run; if clean (no SFE-1994), declare done; if not, debug from there.**

**5 commits ahead of origin/main not pushed:**
- `503fa0b` v1.2 stage-1: ic_from schema + DECISIONS #57 + IC injection via additionalArgs
- `1296cc4` v1.2 stage-2: per-corner submit (correct mechanism but N Maestro histories)
- `61b554a` v1.3: single-batch + Maestro pre-run script (ONE history, GUI consolidated)
- `fc574bc` v1.3 worker-VM SKILL quirks: `let* → let+setq`, `cons → list`
- `efb4cac` v1.3 syntax fix: `readns="<path>"` (netlist) not `+nodeset <path>` (CLI)

**Earlier in this session (now stale — kept for reference):** Phase 3A v1.2 was originally going to be Maestro post-Spectre flag-stickiness mitigation (re-verify after #56 cwd fix; Spectre dispatches but Maestro keeps the "actively running" flag → cleanup hits ASSEMBLER-2423). User PROMOTED the trans→PSS IC chaining workflow into v1.2 instead. The original v1.2 candidates (flag-stickiness + push --replace + per-corner verdict) reslot to v1.4+. Old v1.2 options for the cleanup-modal problem, in case someone picks it up later:
- **(a) wait for worker PID exit**: `pvt_runner_run` accepts a `wait_for_worker_exit_sec` kwarg; after status-idle, polls `ps -p <axl-worker-pid>` until it's gone. Needs SKILL helper to expose the worker PID.
- **(b) read history's done-marker**: probe whether `axlGetCurrentHistory`'s handle exposes a "completed/error/running" slot (earlier probes didn't find one, but the rdb walker via `_pvtCollWalkRdb` does emit per-row status; if ALL rows are `'done`/`'failed`/`'eval_err`, treat history as done).
- **(c) treat post-rename cleanup as best-effort**: catch ASSEMBLER-2423 in `pvt_runner_delete_history`, retry with exponential backoff up to N seconds, surface as a warning not an error. Pragmatic but pushes the problem to runtime.

**Pick after the next real PVT dogfood** (when you push a clean union that doesn't hit Spectre netlist errors): observe whether (a)/(b)/(c) is the natural answer.

**v1.2 #2 (rdb-walker discriminator):** **DEMOTED** from HIGH priority. `axlGetRunStatus` returning `[0,0]` post-Spectre-exit was always semantically correct. A rdb-walker count-of-running discriminator is still a robustness improvement (catches sessions where axlGetRunStatus might behave differently across Cadence versions), but no longer urgent. Pick up only if v1.2 #1 reveals the discriminator IS the bottleneck.

**v1.2 #3: `pvt corners push --replace` mode** — S2's push of a 3-row union resulted in 6 corners (3 existing + 3 new added). Phase 2 §4.2 ambiguity; resolve before more corner-mutating dogfood. v1.1 verify worked around by using `axlRemoveElement` for cleanup directly (which worked even with the setupdb lock — interesting data point per #55).

**v1.2 #4: per-corner failure detection + strategy chain wired in `execute()`** — §4 plumbing (Strategy base + naive_retry) landed; what's missing is post-PvtSave query against the just-ingested DB rows to identify FAIL corners + dispatch the strategy chain. Depends on v1.2 #1 being solid (run-completion signal must be reliable).

**v1.2 #2: `pvt corners push --replace` mode** — S2's push of a 3-row union resulted in 6 corners (3 existing + 3 new added). Phase 2 §4.2 ambiguity; resolve before more corner-mutating dogfood. v1.1 verify worked around by using `axlRemoveElement` for cleanup directly (which worked even with the setupdb lock — interesting data point per #55).

**v1.2 #3: per-corner failure detection + strategy chain wired in `execute()`** — §4 plumbing (Strategy base + naive_retry) landed; what's missing is post-PvtSave query against the just-ingested DB rows to identify FAIL corners + dispatch the strategy chain. v1 ships without it; user reads `pvt list` for now. Depends on #1 being solid because the run-completion signal must be reliable before per-corner verdict reading is meaningful.

Then v1.3: `asi*` probe phase → `gmin_bump` + `trans_pss_ic` built-in strategies (DECISIONS #52).

**v1.1 cleanup owed to user:** the live verifies (2 rounds) left 4 orphan histories on `fnxSession0` that are still locked by Maestro: `Interactive.0`, `Interactive.1`, `orch_v11_real_1778936686`, `orch_v11_real_1778939316`. Clear via Maestro GUI History panel → right-click delete once underlying sims are truly done (the worker shows "Failed" in the GUI once Spectre's netlist error propagates — that's the safe-to-delete signal). Cornerwise session is at 3-row baseline (`TT` / `TT_pvt` / `TT_2p5G`).

**Spec §10 open decisions — closed/partial by 2026-05-16 dogfood:**
- **10.1 RunStatus codes** — partial: observed `[0,0]` idle, `[1,1]` cached-done, `[5,9]` real-run-completion. Full code map still needs more observation.
- **10.2 axlRunAllTests arity** — CLOSED: `(string sess, string x)`, where x is NOT a history name. Always auto-names `Interactive.NN`; rename via `axlSetHistoryName` post-run.
- **10.3 window focus** — CLOSED: bridge can dispatch axlRunAllTests from CIW context fine; the actual issue is only with `axlGetWindowSession`'s session-detection (workaround: pass session name explicitly to every helper).

_2026-05-16 v1.5 #3 + #4 push (1 combined commit `a935101`): #3 added optional `specs: list[str|null]` parallel array on swept `MeasureApply` entries (mutex with single `spec`, length-matched to `output_names`); renderer plumbs per-row spec via existing `RenderedRow.spec`. #4 bumped `signal_group_schema_version` accepted set to `{1, 2}`; v2 items may be `{net, alias}` objects with the alias replacing basename in rendered output names — solves the v1.1 `dco2g_supplies` /VDD×4 basename-collision case. `SignalGroup.signals` now `tuple[Signal, ...]`; 2 production renderer callsites + 3 test files updated. v1 bare-string form unchanged. Python 773 → 797. DECISIONS #48 + #49._

_2026-05-16 v1.5 #2 fix (1 commit `0872878`): inclusive `>=` / `<=` bundle specs now dispatch to `axlAddSpecToOutput ?range` with a `_PVT_MEASURE_SPEC_HUGE = 1.0e30` sentinel, not `?min` / `?max` which Maestro interpreted as ADE-XL target-style and would flip pass/fail. SKILL parser tags renamed `min`/`max` → `ge`/`le` to match Python `spec_eval` convention. Live-verified on fnxSession0: `>= 1e-10` on Rtime_clkout → CSV `range 1e-10 1e+30`; `<= -100` on PN_1M → `range -1e+30 -100`; Python `evaluate_spec` correctly classifies 2.13e-11 as fail and -167 as pass under inclusive-bound intent. Tier-1 394/1 unchanged (2 assertion strings updated). DECISIONS #47._

_2026-05-16 v1.4 push (4 commits): #4 X..Y parser, #5 apply CLI spec_status, #1 pass/fail capture (SKILL collector → ingester → schema → CLI), docs sweep. Live dogfood on fnxSession0 / simkit_check_spec produced run.json with `schema_version:2` + `output_specs:{"Test":{"Rtime_clkout":"< 1e-10","PN_1M":"> -150"}}`; ingest + query showed Rtime_clkout=2.13e-11 → pass, PN_1M=-167 → fail, `pvt list` "1/2 FAIL", old v1.3 run still "—" (NULL spec_status, not 'no_spec'). v1.4 #3 (weight pull) was scope-shrunk per DECISIONS #46 — weight is readable but user confirmed they never touch it; deferred to v1.5. fnxSession0 cleaned: the 2 dogfood specs (Rtime_clkout, PN_1M) wiped via `axlOutputsImportFromFile ?operation "overwrite"` with blanked Spec columns — the same path `pvt measure restore` already uses (DECISIONS #46 Decision G). 11-row baseline restored, `_pvtCollCaptureSpecs` returns empty table._

## What's NEXT (the deferred-work shelf)

**Phase 3A v1.5 is DONE end-to-end, `pvt run` is now a real entry point.** As of 2026-05-18 PM: CLI wiring + F1 (validator pending) + F2 (rdb-walker discriminator) all landed and live-verified twice on `fnxSession0`. Together with v1.4 (zero-click batch via worker-skip), `pvt run reviews/*.review.json --session fnxSession0` is the engineer-facing entry for an N-item signoff battery. Per-item snapshot/restore + per-sub-point completion gating + per-run ingest are all auto.

**Owed back from user (housekeeping, GUI-only):**
- Maestro GUI: delete v1.7 A5 + A7 dogfood histories — `gmin_scope_probe_1779085991` (A5 Phase 1 pre-fix probe), `v17gmin_v17_gmin_demo_1779086137_1` + `v17_gmin_demo__gmin1` + `v17_gmin_demo__gmin2` (A5 Phase 2 chain dogfood + 2 retries), `gmin_scope_v2_probe_1779086794` (A7 post-fix Phase 1 verify). All ingested + safe to wipe.
- Earlier v1.5 dogfood histories from PM: `v1dog_Test_on_baseline_1779081377_1`, `v1dog_Test_trans_on_baseline_1779081377_2`, plus 4 `orch_Test*` / `orch_Test_trans*` from F2 live-verify rounds.
- v1.4 histories from AM that may still be undeleted: `v14dog_pss_v14_1779068447_1`, `_1779072455_1`, `_1779078345_1`, plus `Interactive.3`, `wedge_diag*`, `wedge_probe`, `patch_v`, `patch_v2`.
- Pre-run script attached-but-disabled on `Test` + `Test_trans` from A7 verify (Maestro has no detach API; harmless because the enabled flag is checked before each firing). If cosmetics matter: ADE-XL → test menu → Pre Run Script → Detach. Otherwise leave; on next `/tmp` wipe it auto-deactivates as a missing-file warning at next install.
- `/tmp` housekeeping for today: `/tmp/pvt_run_live*.log`, `/tmp/probe_session_state.py`, `/tmp/probe_sessions.py`, `/tmp/pull_baseline_union.py`, `/tmp/cli_test_run.log`, `/tmp/strace.out`, `/tmp/probe_gmin_phase1_*.py`, `/tmp/probe_gmin_phase2_chain.py`, `/tmp/probe_gmin_phase1_v2_scope.py`, `/tmp/v17_gmin_dogfood.review.json` — all probe leftovers, safe to rm anytime.

**Today's deliverables (2026-05-18 EOD) — quick lookup:**
| Commit | Repo | What |
|---|---|---|
| `9b5328f` | simkit | Phase 3A v1.4 baseline-corner. (AM) |
| `a69bf12` | simkit | DECISIONS #60: skillbridge worker-VM socket-steal. (AM) |
| `e8c76e9` | skill_tools (sibling) | `sbStart.il` axl-worker detect + skip pyStartServer. (AM) |
| TBC | simkit | Phase 3A §5 close: `pvt run` CLI ↔ `execute()` wiring. (PM) |
| TBC | simkit | Phase 3A v1.5 F1+F2: validator `pending` whitelist + `pvtRunnerCountRunning` rdb-walker discriminator + `pvt_runner_run` state-machine AND-gate. (PM) |
| TBC | simkit | Docs closeout: PROJECT_STATE + TODO + DECISIONS #61 + memory `feedback-pytest-sysmodules-mock-trap`. (PM) |

**Surfaced during v1.5 dogfood, NOT in scope today (queued for v1.6+):**
1. **run.json `history_name: None`** — PvtSave on the SKILL side doesn't write the passed `histName` into the envelope. Cosmetic, but breaks `pvt list/diff` history-name correlation. 1-line SKILL fix in `skill/pvtCollect.il::PvtSave`.
2. **pytest sys.modules-None mock trap** — `mock.patch.dict(sys.modules, {"pkg.name": None})` doesn't block `from pkg import name` once cached on pkg, can leak real SKILL calls from tests into a live Maestro session. Pinned in memory [[feedback-pytest-sysmodules-mock-trap]]. Audit other tests for the same anti-pattern when time permits.

**v1.3 known gaps (truly bite-when-they-bite, not blocking):**
1. **`additionalArgs` prior-value snapshot/restore** — orchestrator captures & restores the user's prior PRE-RUN SCRIPT but NOT the prior `additionalArgs` simoption value. v1.3 pre-run overwrites it per-corner; cleanup clears to `""`. If user has a non-empty manual additionalArgs (e.g. `+log debug`), it gets clobbered. Fix is ~20 lines (mirror `prior_scripts` capture with `prior_addl_args` from `asiGetSimOptionVal`).
2. **Multi-test consumer items** — v1.3 picks `item.tests[0]` as the IC-resolution test. Multi-test items with different signal nets would need per-test pre-run scripts.
3. **Per-corner→per-corner chains** — `_resolve_ic_path` expects upstream = batch (one history with N corner subdirs). Should work for v1.3-downstream-of-v1.3 since v1.3 also produces single batch, but confirm during a 3-item chain dogfood.

**Original Phase 3A v1.2 candidates (now mostly closed via v1.5):**
- ✅ v1.2 #1 (rdb-walker discriminator) — DONE in v1.5 F2 / DECISIONS #61.
- ❌ v1.2 #2 (`pvt corners push --replace`) — still open. ADD-semantics surface can produce duplicates.
- ❌ v1.2 #3 (per-corner verdict + strategy chain dispatch) — still open. §4 strategy plumbing is there; what's missing is the post-PvtSave DB query to identify FAIL corners + dispatch chain. Now unblocked by F2's reliable completion signal.
- ❌ `runTests.il` atomic loader fix — separate pre-existing breakage; pin as tools-cleanup.

**Operational notes for next session:**
- **Auto-reload SKILL files via bridge — do not ask user to click into CIW.** The bridge can fire `ws['load']("/abs/path/to/file.il")` directly. Iteration on `.il` files no longer needs user `(load ...)` typing; only fall back to asking the user when the bridge itself is dead. Memory updated in `reference_skillbridge_recovery.md`.
- **Bridge bootstrap is now ONE focus per session.** Cache sdb at start of any probe via `skill_bridge.get_sdb(name)`; pass the returned int as `session=` to all read-side wrappers. Run-side wrappers (`pvt_runner_run` / `_submit` / `_get_status` / `_rename`) still need the session-name string — and with DECISIONS #60 fix in place, name resolution survives axl dispatch (no more focus dance).
- **`get_sdb` translates 3 bridge failure modes** into clear `SkillBridgeError` messages: `bridge_wedge` (unpack-mode → `(pyKillServer)(pyStartServer)` in CIW), `bridge_dead` (socket lost → shell kill stale processes + fresh `(pyStartServer)`), `session_focus_lost` (Maestro not focused → click it). With DECISIONS #60 fix the `session_focus_lost` flavor should be rare to absent.
- **Multi-pair stacking trap — root cause identified 2026-05-18.** Was NOT pyKill/pyStart misuse; was axl worker virtuoso processes auto-loading `sbStart.il` and clobbering the socket binding. **Fix is now in `sbStart.il` itself** (see commit `e8c76e9` in sibling `skill_tools` repo). If you ever see >1 pair in `ps -ef | grep python_server.py`, the new normal interpretation is "user fired `(pyStartServer)` manually more than once OR `sbStart.il` was reloaded by the bridge"; orphan workers no longer accumulate. Cleanup as before: `pkill -f "python_server.py default INFO" && pkill -f "cdsServIpc.*skillbridge_skill.log"` then one `(pyStartServer ?python "/usr/bin/python3")` in CIW.

**Phase 3A v1.3 mechanism cheatsheet (for next session):**
- Generator: `simkit/pre_run_script.py::write_pre_run_script(PreRunSpec, workdir)` → writes `.simkit/pre_run/pre_run_<item>_<hash>.il`
- Bridge wrappers: `pvt_runner_install_pre_run_script` / `_disable_` / `_get_` (need session NAME, not sdb)
- SKILL: `pvtRunnerInstallPreRunScript` / `_Disable_` / `_Get_` in `skill/pvtRunner.il`
- Script template: `(let ((cornerName nil) (cornerMap (list (list "TT_pvt_0" "readic=\"/path\"") ...))) (setq cornerName (car (errset (axlGetCornerNameForCurrentPointInRun) nil))) (when ... (asiSetSimOptionVal asi "additionalArgs" (cadr entry))) t)`
- Worker-VM SKILL quirks: NO `let*` with errset-wrapped initials; NO `(cons k v)` for non-list v; ALWAYS return `t` (nil aborts that corner)
- additionalArgs is appended into netlist's `simulatorOptions options` block (NOT spectre CLI) — value syntax is `readns="<path>"` / `readic="<path>"`
- For sweep-row corners (1 row with internal DV sweep + multi-section model file): `axlGetCornerNameForCurrentPointInRun` emits the **exploded sub-point name** (e.g. `TT_pvt_0..5` for a 2 VDD × 3 section row, 6 total). VDD inner-sweep doesn't fire its own pre-run (handled inside Spectre's parametric sweep). Confirmed by diagnostic log 2026-05-17.

**Architectural paths explored + rejected today (don't re-walk these):**
1. **Per-corner additionalArgs API (Option D)** — Maestro doesn't expose one; the Corners Setup form only supports per-corner overrides for Design Vars / Model Files / Device Parameters / Temperature, never simulator options. Two Explore agents confirmed.
2. **Per-corner Model File include trick (Option E)** — `axlPutModel` registers the entry but Maestro's netlister silently drops it from `input.scs`. Probed with both bare wrapper (no section) and `section simkit_ic ... endsection` wrapper + `axlSetModelSection`; both registered post-add but neither emitted include. Maestro likely has implicit validation that filters non-PDK-shape model files.
3. **Per-sub-point Model File (Option E')** — same Maestro-netlister filtering as E above, plus would need to explode `TT_pvt` sweep-row into 6 standalone rows (heavy corner-table mutation).
4. **Per-corner submit + GUI grouping (Option A')** — Cadence's `axlGetHistoryGroup*` API is read-only; there is no `axlPutHistoryGroup` setter to merge N siblings under one parent. So N submits = N siblings in the GUI, can't paper over.
5. **Conclusion (DECISIONS #58 companion finding):** v1.3 pre-run-script remains the only viable architecture for per-sub-point IC delivery; A/B test proved the serial-dispatch cost is Maestro's local-sim default, not pre-run's fault. (The same A/B test had also exonerated multi-rf018.scs, but **that finding was wrong** — retracted 2026-05-18 after user pushback with simkit_verify evidence. Root cause is `_prep` over-disabling scalar corners → Maestro inserts a `nom` baseline subdir → all-section include. Fix queued as v1.4 baseline-corner preservation, see [[project_baseline_section_count]] for the env-dependent section count.)

**Phase 3B v1.5 follow-ups (parked until they bite):**

1. **Dogfood v1.5 #3 + #4 on a real bundle** — neither feature was live-verified after landing (pure Python loader/renderer change, SKILL surface untouched). Pick up when you write the first real bundle that needs per-iteration sweep specs OR an aliased signal-group. Path of least resistance: revisit `~/cadence_work/simkit_p3b_dogfood/` and try a `PN_*` sweep with per-frequency specs + the dco2g_supplies group expressed in v2 alias form.
2. **Phase 3B v1.5 #5 — multi-axis `param_sweep`** — only ever needed if a real "freq × temperature" 2D output table case appears (per 2026-05-16 conversation, user expects temperature to live on the PVT-corner axis instead, so this is likely never needed and can be deleted from PHASE_PLAN at the next sweep).

**Phase 1 / Phase 2 backlog (continues — no urgency):**

1. **Use the v1.1 builtins in daily work + flag what's missing** — install into one real `.pvtproject`, run `pvt measure install-builtins`, author a bundle against the live testbench, observe what bites. The two biggest pain points from the original walkthrough (basename collision + per-frequency PN specs) are now addressed by v1.5 #4 + #3; remaining likely gaps are around list-templates UX and the first user-authored derivative.
2. **GUI Import verification (user, when convenient)** — confirms the truly-skillbridge-independent recovery path for Phase 2 (Maestro `Tools → Corners → Import` on the `pvt corners build` output). Skillbridge `restore` is already live-verified, so this is just the GUI-decoupled fallback check. Optional but completes the Phase 2 recovery story.
3. **Open Decision 8.6 — explode order at multi-axis scale** — Gate U2 closed at 21-row × 1-axis-per-row scale (synthesised); Gate U3 closed at 2×3×5 = 30 small synthetic scale. Multi-axis (e.g. 5+ sweep fields in one row) at real-bench scale still untested. Park until a real VCO LO setup is available.
4. **CSV format edge cases** — emitter is v1 (single-test, single-model-per-row, no quoting). If a real session ever has a var/section/path with comma/quote/newline, `pvt corners build` raises `CsvBuildError`. Add a v2 quoting path then. Multi-test setups also need per-(corner, test) enable matrix — additional schema work.

## Phase 1 backlog (continues)

- §2.2 dialog Tier-2 manual UI verification (5 scenarios at `skill/tests/tier2/scenarios.md`).
- §3 walker mock-rdb harness (DECISIONS #23).
- §3 screenshot v1.1 (S3_DESIGN §3.5).
- **§3 messy-data Tier-2 against the new histories** — `simkit_simerr` (all-sim-err) and `simkit_Rtime_err` (one-corner eval-err) are live in `fnxSession0`. When picking up Phase 1 §3 (c), call `PvtSave(?histName "simkit_simerr")` and `PvtSave(?histName "simkit_Rtime_err")` from CIW or skillbridge; eyeball the resulting `run.json`s with `pvt validate <path>` to confirm pass-2 / pass-3 row shaping.

**Backlog (deferred from Phase 1, do alongside if convenient):**
- §2.2 dialog Tier-2 manual UI verification (5 scenarios in `skill/tests/tier2/scenarios.md`).
- §3 walker mock-rdb harness (DECISIONS #23).
- §3 screenshot v1.1 (S3_DESIGN §3.5).

## Open questions / blockers

- **JSON byte representation** — `pvtJson.il` uses a precomputed 255-byte octal LUT to synthesize bytes (because classic SKILL `sprintf "%c"` rejects integers and there is no `intChar`). Side effect: **JSON strings cannot contain ` `** by this implementation. RFC 8259 allows it, Python stdlib accepts it. Not a blocker for `.pvtproject` (no NUL ever), and probably not for collector dumps either, but flag if §3 ends up needing to round-trip arbitrary user-supplied strings.

## Context cheatsheet for fresh sessions

- **User**: analog circuit designer; Cadence Virtuoso ICADVM18.1-64b; Python 3.11.4.
- **Environments**: home = dev (Claude Code OK, mirrored Cadence); work = red zone (offline only, no Claude Code). Deploy constraint: fully offline-installable.
- **POC file**: `../MyRunner/PvtDumpToJson.il` — proved dump path works. **Do NOT extend it**; Phase 1 writes a clean collector from scratch (Decision #12).
- **SKILL reference docs**: `../SKILL_file/` — 44 Cadence PDFs organized by topic. Consult before writing SKILL. The standing rule (`virtuoso-skill` skill) is mandatory for any SKILL coding task.
- **SKILL test infrastructure**: skillbridge is running on this machine (socket `/tmp/skill-server-default.sock`, install at `../skill_tools/skillbridge/`). Use it to verify SKILL idioms BEFORE assuming they work. See Decision #14 for the general classic-SKILL trap list and Decision #16 for the `axlrdb*` slot-accessor funobj rule (specific to live ADE-XL session code).
- **Tier-1 vs Tier-2**: Tier-1 (`/tmp/run_skill_tests.py`, 168 tests) covers pure helpers and is mock-free. Tier-1 is necessary but **not sufficient** for any code that touches a live ADE-XL session — see Decision #16 for the canonical "Tier-1 green, live silent-fail" episode. Mandatory Tier-2 verification against an open Maestro session before declaring any §3-style feature done.
- **Test driver**: `/tmp/run_skill_tests.py` (transient — `/tmp` may be cleaned). Pattern is documented in `skill/tests/README.md`; rebuild from there if missing.
