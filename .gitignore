# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
.env
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 3-zone deployment cache — wheels are downloaded fresh on yellow,
# bundled into tarball, restored on red. No need in git.
vendor/wheels/

# skillbridge runtime logs (created by Virtuoso when bridge starts)
skillbridge_*.log

# DuckDB / databases (runtime data, not source)
*.duckdb
*.duckdb.wal
*.db
*.sqlite
*.sqlite3

# Cadence working files that may land here during dev
*.ahdlcmi/
*.raw/
CDS.log
panic.log.*
simulation/
li.log*

# Cadence tooling cache + personal run logs (not source)
.cadence/
logs_yusheng/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
.claude/
*.swp
*.swo
*~

# Local config overrides (the tracked example lives in config/)
.pvtproject.local
